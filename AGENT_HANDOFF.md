# AGENT_HANDOFF.md — Live Session State

> **Read this first every session** — this file is now the compact live-state view (trimmed 2026-07-12
> from 270KB; full history moved to `AGENT_HANDOFF_ARCHIVE.md`, pull it up only when you need forensic
> detail on a specific past feature/incident, not by default).
> Then `ONBOARDING.md`. `AGENTS.md`/`CLAUDE.md` are the rulebook, not session state — read on demand.
> Protocol: `SESSION_MEMORY.md`.

---

## CURRENT STATE

- **2026-07-28 released Fiat/Keno/notification read resilience:** every read owned by the released surfaces now uses the bounded connection-only PostgreSQL retry: Fiat overview/access/webhook reads, Keno operations/dashboard KPI reads, and notification recipient search. Direct-notification docs no longer describe a retired dev-only gate, and the production release contract fixture locks the resolved environment behavior. Vercel production has the backend URL/key, writable MAIN client, read mirror, Admin DB, and gift-card pepper bindings. A read-only live mirror probe was rejected with SQLSTATE `53300` (`too many connections for role "fraud_app"`), matching recent production logs; no database capacity or data was changed. All 276 guardrails, focused retry/release tests, TypeScript, zero-warning ESLint, diff check, and the production build pass.

- **2026-07-28 navigation unseen badges:** Main Fiat plus Antifraud Fiat Deposits, Signups, and Account Review show per-staff unseen counts from visible-tab 60-second polling and clear on visit. Server-issued upper-bound watermarks prevent staff clock skew and count/seen gaps; a five-second safety lag covers read-replica delay, request sequencing prevents stale polls from restoring a cleared badge, and cross-tab storage changes refresh visible tabs. Fiat counts use the true completion time across completed/refunded/disputed lifecycle states while retaining the existing status/updated index (read-only dev-mirror plan: 0.375 ms). Counts are capped at 99+, bounded to 30 days, and preserve the last known badge through transient failures. All 276 dashboard guardrails, TypeScript, zero-warning ESLint, the production build, and all 95 monitor checks pass.

- **2026-07-28 Keno navigation order:** Keno now appears in Overview directly below Fiat instead of under Content. TypeScript and task-scoped ESLint pass.

- **2026-07-28 Keno configurable max bet:** Content → Keno → Configuration now reads and updates the live `keno_max_bet_usd` value through `GET/PUT /v1/admin/keno-config`, validates the backend's $0.25–$1,000 range, audit-logs changes, caches production reads, and hides the raw key from the generic Security editor. Production currently returns $20. Focused tests, all 268 guardrails, TypeScript, zero-warning ESLint, and the production build pass.

- **2026-07-28 Account Review opener visibility:** `/antifraud/reviews` now identifies the staff member who opened each case instead of showing assignment state. Automatically ingested cases are labeled `Antifraud monitor`, and unresolved deleted staff identities fall back to `Unknown staff`. Focused ESLint and TypeScript pass.

- **2026-07-28 Antifraud Discord alert redesign:** Score-60 signup alerts now show separate account, risk, trigger, top signal, and case sections with severity-aware accents and a direct Review case button. Rule-match alerts use the same clean layout and add score change plus outcome. Mention allowlisting and durable delivery are unchanged. The monitor passes 91 tests; dashboard TypeScript and focused zero-warning ESLint pass.

- **2026-07-28 fiat-threshold holds in Account Review:** Account Review queue, case summary, and related-signal rows render a clear “Fiat-triggered withdrawal hold” badge. The repository-contained Antifraud monitor now detects complete automatic lifetime-deposit withdrawal locks from the read-only MAIN mirror, commits a high-severity signed review event plus a separately retried Discord outbox row, and advances its source cursor in the same transaction. The dedicated webhook uses the three compiled standard support mentions and links to `/antifraud/reviews`; its runtime presence is exposed without returning the URL. Production mirror evidence found 51 `user_feature_locks` rows (80 KiB), zero current automatic holds, and a 0.407 ms exact-query execution, so its sequential scan is currently preferable to another index. The monitor passes 95 tests; dashboard TypeScript, zero-warning ESLint, all 265 guardrails, and production build pass.

- **2026-07-28 Creator leaderboard workspace ownership:** Leaderboards are exposed only through the Creators app sidebar. The duplicate normal Admin sidebar entry is removed, and the leaderboard-prize breakdown now links to `/creator-hub/leaderboards`. A focused ownership guardrail, TypeScript, and task-scoped zero-warning ESLint pass.

- **2026-07-28 Whop fiat, physical-withdrawal, and geo-policy hardening:** Geo Blocking has three compact global controls on one desktop row. The fiat switch moves direct card, Apple Pay, Google Pay, and both Cash App lock aliases together across the site and every location while preserving all 33 mandatory exclusions. The new physical-item switch changes only `country_restrictions.physical_withdrawal` across every location; it does not touch crypto, balance, fiat, or the retired all-withdrawals master flag. Production was explicitly set OFF on 2026-07-28: 55 enabled rows were changed, all 305 rows verified disabled, the exact 55-code recovery set was retained in the operation transcript, and the backend country-restriction cache reload returned HTTP 200. The Vercel data-cache key was advanced to v2 after the first release exposed its pre-mutation 55/305 snapshot across deployments; the page now fills from the verified 0/305 source. The 33-location full geo-block switch keeps the same complete wallet lock. Bulk actions await backend cache reload attempts and warn instead of claiming the change is live when runtime cache reload fails; the manual reload button refreshes both site config and country restrictions. After production logs showed both tiny Geo reads plus an unrelated header read failing together on a transient MAIN-mirror connection timeout, the two read-only Geo cache fills gained one connection-only retry and the five-minute warmer now keeps both keys hot; SQL/schema/permission failures remain single-attempt. All 272 guardrails, TypeScript, task-scoped zero-warning ESLint, diff check, and the production build pass.

- **2026-07-28 Pack Builder strict production math:** New Pack Builder production writes now fail closed unless the solver output and the final persisted card rows each total exactly 1,000,000 integer ticket units (100.0000%). The inclusive house-edge band is narrowed to 10.95%–11.50% across client controls, queued-request validation, preview, saved-build promotion UI, owner review UI, and the fresh final production solve.

- **2026-07-28 Fiat, Keno, and personal-notification completion:** production contract probes confirm both personal-notification routes are live, the Fiat/payment/Keno/notification relations are present on the read-only mirror, and the production reward-code pepper is configured. `/notifications` now follows the resolved prod/dev backend, identifies the exact target, confirms production sends, starts custom composers blank, and permits reward campaigns only when the notification backend and writable game DB resolve to the same environment. `/keno` uses canonical customer scope for every aggregate and adds 25 recent settled rounds; the production mirror returned 253 customer games and the read-only recent-round plan completed in 0.661 ms. `/fiat` now exposes recorded customer-paid, credited, provider-net, fee, intent-freshness, and webhook-freshness values. Missing wager-default response keys stay visibly disabled instead of being fabricated as 1×, while every supported default field now passes server validation. All 251 guardrails, TypeScript, full zero-warning ESLint, the production build, and live read-only contract/query checks pass.

- **2026-07-28 Pack Builder production-image gate:** Pack Builder drafts may still be saved without artwork. A valid image is now required at direct live-request submission, saved-draft promotion, and final owner approval; the Approval Queue also disables image-less approvals. Saved Builds exposes an authenticated, ownership-scoped image upload/replace action so an incomplete draft can become publishable without being recreated. Focused checks (9/9), all 247 guardrails, TypeScript, zero-warning ESLint, and the production build pass.

- **2026-07-28 Pack Builder saved-build access:** `/pack-studio/builder-drafts` is now the clearly named Saved Pack Builds workspace. Pack Builders see only their own saved builds, matching the existing ownership checks on submit/discard; owners and admins retain the full shared view. Pack Studio now supplies route-specific browser titles through its own metadata template. The focused pack-flow checks (8/8), all 246 guardrails, TypeScript, zero-warning ESLint, and the production build pass.

- **2026-07-28 score-60 signup review delivery:** Every signup assessment at 60+ writes a dedicated durable Account Review marker into the signed risk-event stream and a separately retried Discord webhook alert. Account Review exposes Fine, Ban, and Lock withdrawals as one-click actions with a single confirmation and no separate 2FA prompt, while retaining capability checks and audit records.

- **2026-07-28 signup/live behavior scoring:** 3+ signups on one IP in 10 minutes now scores 60, generated usernames 25, country bursts 25/50, and proxycheck risk bands 40/80; missing email is retired. Live monitoring removed the impossible unclassified-deposit path, added battle/upgrader activity, and replaced generic reward/bonus events with the real welcome, Level 1, daily 10–100, rakeback, rain, deposit-bonus, race, creator-leaderboard, challenge, tip, and sponsored-battle behaviors. Reward-before-deposit alone is neutral; welcome-reward gambling/Level 1 stacking and tip/sponsored-battle receipt before deposit are dedicated review flows. The live mirror confirmed the reward IDs and zero unclassified deposits in the recent 30-day window. Monitor TypeScript and all 83 tests plus task-scoped zero-warning ESLint pass; migration `014_signup_live_behavior_tuning.sql` applies the production weights and flows.

- **2026-07-28 withdrawal funding clarity:** `/antifraud/withdrawals` now presents each funding review as three numbered steps: latest funding origin, activity after that origin, and the withdrawal. The stale 90-day wording is removed from list and detail surfaces. Balance withdrawals no longer require card/voucher attachments and no longer receive the false `amount_mismatch` + `no_assets` 80-point penalty; physical and crypto requests retain exact asset reconciliation. The list hides irrelevant asset-origin evidence for balance payouts, while detail explicitly marks it not applicable. All 82 monitor tests, 242 dashboard guardrails, TypeScript, zero-warning ESLint, monitor build, and the production dashboard build pass.

- **2026-07-28 New Packs request previews:** `/pack-studio/new-packs` now shows each submitted pack image and the same four-segment player-facing risk bar used by Pack Builder, with the filled level stated explicitly as `N/100`. Missing or zero difficulty remains honest: `0/100` with the on-site bar identified as hidden. Approval and decline behavior is unchanged. The focused approval guardrail, all 243 guardrails, TypeScript, zero-warning ESLint, and the production build pass.

- **2026-07-28 fiat risk score guide:** `/antifraud/fiat-deposits` now has a fifth summary card with a segmented score bar and the exact assessment bands: 0-29 Good, 30-59 Review, and 60-100 High risk. The loading state matches the five-card layout. Focused fixtures, all 237 guardrails, TypeScript, zero-warning ESLint, diff check, and the production build pass.

- **2026-07-28 dashboard KPI/Trends resilience:** `/dashboard` no longer loads Trends through the oversized all-purpose stats aggregate or lets one failed query replace an entire section. KPI and trend snapshots retain the last complete production result for 24 hours in the existing shared cache, mark the confirmation time when served stale, isolate cold failures to the affected tile, and cap trend/reward query fan-out at two. The regression fixture, all 240 guardrails, TypeScript, scoped zero-warning ESLint, diff check, and the production build pass.

- **2026-07-28 signed Antifraud ingest delivery:** `antifraud-monitor` now delivers committed `risk_events` to the Admin dashboard's `/api/antifraud/ingest` route in HMAC-signed batches of at most 50. A dedicated Antifraud-DB cursor advances only after every event is accepted or confirmed as a duplicate; event ids make crash retries idempotent, a PostgreSQL advisory lock keeps multiple replicas single-sender, and transient failures back off without blocking the monitor poller. The Settings page reports both receiver and sender configuration instead of treating a receiver-only secret as healthy. Monitor TypeScript/build and 80 tests pass; dashboard TypeScript, zero-warning ESLint, all 237 guardrails, and the production build pass.

- **2026-07-28 signup age visibility:** every account row on `fraud.packydash.com/signups` has an aligned compact cyan `Signed up 40 min ago` identity badge; the exact Europe/Berlin timestamp remains in its hover title. The badge stays inside the existing identity column instead of adding row height or shifting the risk/provider/action columns. The focused fixture, all 238 dashboard guardrails, TypeScript, and scoped zero-warning ESLint pass.

- **2026-07-28 MAIN mirror routing shipped:** ordinary production/development MAIN reads fail closed onto `MIRROR_PRODUCTION_DB` / `MIRROR_DEV_DB`; explicit mutation clients still use `DATABASE_URL` / `DEV_DATABASE_URL`. Vercel production deployment `dpl_GEJjyqHjmqEVqo9DV1UwfwY7rd6G` is READY on all four dashboard domains, and the authenticated PostgreSQL health check reaches the production mirror. Railway production `antifraud-monitor` now reads `116.203.238.58:5434` with encrypted standard-libpq `require` semantics; durable URL normalization prevents `pg` from silently restoring certificate verification, deployment `68c06912-5fcf-43b7-a3d6-e75d9adc31ea` is SUCCESS, `/health` is 200, and the poller is healthy with zero consecutive failures. The dev mirror has all 60 reviewed indexes valid (15 Antifraud + 45 other), no invalid public indexes, and no obsolete unsafe IPv6 index. The production mirror credential is intentionally read-only (`CREATE=false`, owns 0/94 tables), so no custom production index DDL was possible or attempted. Validation: 232 dashboard guardrails, TypeScript, ESLint, production build, and the monitor TypeScript plus 69 tests pass.

- **2026-07-28 fiat deposit risk workspace:** `fraud.packydash.com/fiat-deposits` is a dedicated Transactions workspace backed by persisted Antifraud assessments. It combines sanitized Whop risk/3DS/refund/dispute evidence, prior crypto and fiat funding, attempt velocity, account/KYC state, shared device/signup-IP links, and post-deposit play-through/cash-out behavior into six explainable checks and a 0-100 good/review/bad score. The queue, filters, counts, and volume include only paid lifecycle states (`completed`, `partially_refunded`, `refunded`, `disputed`), never checkout-created/pending/failed/canceled attempts. Money trail, analyst review history, and idempotent clear/escalate/hold recommendations are included; review actions do not move money or write MAIN. Excluded users and creator-role accounts are rejected at source, list, detail, and mutation boundaries. Monitor checks pass 76 tests; dashboard TypeScript, scoped zero-warning ESLint, all 237 guardrails, focused fixtures, and the production build pass.

- **2026-07-28 creator affiliate-fraud scope:** Creator-fraud assessments now treat the creator only as the owner of the affiliate cohort. Self-referral rows are excluded from both cohort membership and affiliate economic aggregates, so the creator account's own behavior cannot affect the score. Dashboard and rule copy now consistently identifies referred-account network, signup, and activity evidence. The monitor service passes 60 tests; dashboard TypeScript, zero-warning ESLint, 230 guardrails, diff check, and the production build pass.

- **2026-07-28 KYC review usability:** `/antifraud/kyc` excludes untouched backend placeholder rows and now lands on the current `kyc_required` queue rather than historical records. Completed cycles remain under clearly labelled history views, and system-configuration/webhook diagnostics are no longer rendered. The production MAIN read confirmed zero currently required accounts and two completed `reason: test` cycles with no Sumsub applicant; MAIN was not mutated. Focused KYC guardrails, TypeScript, and zero-warning ESLint pass.

- **2026-07-28 Antifraud monitor-case recovery:** account-case creation now uses the exact `subject_type = 'account'` predicate from migration 008, fixing PostgreSQL `42P10` failures after the network-case index split. The elected poller retries stored signup dead letters in bounded/capped batches, reconstructs each original three-minute activity window before completion, deletes only committed recoveries, and exposes pending/recovered counts so dead letters can no longer look healthy. Monitor TypeScript/build and 62 tests pass; dashboard TypeScript, zero-warning ESLint, all 226 guardrails, diff check, and the production build pass.

- **2026-07-28 Antifraud withdrawal review scope:** `fraud.packydash.com/withdrawals` excludes Admin-blacklisted accounts and accounts with creator in either the primary or secondary role before source scoring, persisted-list display, detail access, summary counts, or review mutation. The dashboard uses a strict Admin DB exclusion read and the monitor rejects requests without that scope. Money and behavior evidence is no longer a generic 90-day window: every current request starts at the latest completed deposit before it, or the latest completed win/reward/other funding source when no deposit exists, and follows the ledger chronologically to the withdrawal while retaining the origin in long trails. MAIN stays read-only. All 227 dashboard guardrails, TypeScript, zero-warning scoped ESLint, dashboard and monitor production builds, and all 63 monitor tests pass in a clean worktree.

- **2026-07-27 admin/owner passkey grace:** a successful in-app passkey approval now opens a signed, HttpOnly 10-minute no-prompt window across the shared sensitive-action fields for active admins and owners only. Every reuse is user-bound and checks DB-fresh privilege; other roles retain one-action passkey proofs, TOTP stays single-use, role changes stop reuse, and logout clears the window. No schema or email service is required. All 226 guardrails, TypeScript, zero-warning ESLint, and the production build pass.

- **2026-07-27 Pack Builder edge band:** `/pack-studio/builder` accepts only an inclusive 10.95%–11.50% target and resulting house edge. The configured curve is clamped into that range; client validation, queued-request Zod validation, submission preview, and fresh-price owner approval all fail closed outside it. Production ticket rows must total exactly 100.0000%. Focused tests, all 221 guardrails, TypeScript, zero-warning ESLint, and the production build pass.

- **2026-07-27 staff password recovery:** each `/admin-users/[id]` profile now has an admin-driven password reset. It requires the acting admin's TOTP/passkey step-up, uses the shared 10-character/72-byte bcrypt policy, immediately revokes the target's active sessions, protects owner accounts from non-main-owner resets, and writes a password-free audit event. No email service or schema change is required. All 219 guardrails, TypeScript, zero-warning ESLint, and the production build pass.

- **2026-07-27 Pack Builder card controls and odds gate:** `/pack-studio/builder` now keeps card colors and animation switches in client state, carries them through the ADMIN approval request, and writes them when an owner approves the pack. Auto-shaped display odds are rounded to four decimals while remaining exactly 100.0000%; any manual total even 0.0001% under or over blocks submission with the exact total shown. Focused tests, all 216 guardrails, TypeScript, zero-warning ESLint, and the production build pass.

- **2026-07-27 custom Antifraud point-flow builder:** manager-only `/antifraud/flows` now creates and edits ordered, timed player-event sequences with stop conditions, point deltas, manual-review/escalation outcomes, and active/draft state. `/antifraud/events` documents the full live/planned trigger vocabulary, including signup, rewards, pack opening, deposits, rakeback, game, rain, chat, and creator-ledger actions; planned signals can be drafted but cannot be enabled. Every enabled database rule is evaluated by the live monitor, matches once per flow/session, updates the case outcome, alerts through the existing pipeline, appears in Live activity, and is retained on case detail. The monitor shows active flow coverage. Monitor service check passes 54 tests; dashboard guardrails (210), TypeScript, zero-warning ESLint, diff check, and production build pass.

- **2026-07-27 Creator-fraud evidence and network follow-through:** creator assessments now persist safe account-level membership for shared-IP and reused-wallet signals, show the implicated usernames and GGR inputs on the detail page, expose detected IP roots before a snapshot exists, and queue representative account scans after each assessment. Staff-requested scans run ahead of background reconciliation. Account graph links use the normal Admin host. Production Vercel and Railway now share the corrected admin token after its invalid BOM prefix was removed; live Refresh returns `202`, and a seven-account shared-IP graph was confirmed. Service TypeScript plus 55 tests, dashboard TypeScript, full ESLint, all 210 isolated guardrails, and the production build pass in a clean worktree.

- **2026-07-28 Antifraud withdrawal risk v2:** `/antifraud/withdrawals` uses method-aware evidence instead of treating every missing asset as fraud. Balance withdrawals are evaluated from 90-day ledger deltas and never require attached cards; crypto/physical withdrawals still reconcile attached assets. Borrow-mode vouchers are explicit traceable provenance with zero automatic risk. The UI labels cash deposits, gross wagering, play returns/sales, and rewards honestly, reserves red for actual high-risk evidence, and supports the durable analyst workflow/detail view. Migration `015_withdrawal_risk_v2.sql` versions assessments; the monitor backfills all historical requests under a cross-replica advisory lock and refreshes recent requests every minute. Monitor check passes 71 tests; dashboard TypeScript, focused zero-warning ESLint, 234 guardrails, and production build pass.

- **2026-07-27 Marketing affiliate leaderboard restoration:** the existing `/creators/leaderboards` manager is visible in the main/Marketing sidebar again and its dedicated permission key is grantable again. A focused contract guardrail protects the Marketing host, nav, permission, and route gate. All 210 guardrails, TypeScript, task-scoped zero-warning ESLint, and diff check pass; repo-wide ESLint is currently blocked only by unrelated untracked `scripts/.creator-fraud-diagnostic.cjs`.

- **2026-07-27 local repository reconciliation:** stale pre-production branches, worktrees, and stashes were audited against `origin/main`; their changes were already shipped under newer commits, superseded, or intentionally reverted. The only surviving unfinished behavior was the Geo Blocking fiat-baseline counter fix, now on production as `d0c34055`, with focused regression coverage added. The shared staff-notification contract also gained a source guardrail. Guardrails, TypeScript, zero-warning ESLint, and the production build pass.

- **2026-07-27 Antifraud live activity consolidation:** the overview no longer renders its separate player Live Activity card. Pack-opening, upgrader, and battle events now subscribe through the shared Packy live client on `/antifraud/monitor` and appear in that page's existing Live activity log alongside signup, monitor, and matched-flow events. The retired overview-only component was deleted. The focused transport fixture, all 202 guardrails, TypeScript, zero-warning ESLint, diff check, and the production build pass.

- **2026-07-27 Account Review status-only workflow:** `/antifraud/reviews` no longer exposes review severity in queue filters/rows, detail surfaces, manual-case entry, case controls, server mutations, or review KPIs. The unused “Assigned to me” filter is removed, and “Open case” now sits inside the compact filter card beside status controls. Manual cases retain the required internal schema value without making it an analyst-controlled field. Incoming fraud-signal severity remains on the separate overview feed. A focused status-only/layout guardrail, all current guardrails, TypeScript, zero-warning ESLint, and the production build pass.

- **2026-07-27 Account Networks + Creator Fraud:** the dashboard now has API-only `/antifraud/networks` and `/antifraud/creator-fraud` surfaces, grouped together in the Antifraud sidebar's dedicated **Network** section, complete server-side IP/device component traversal with progressive graph pages, masked-IP evidence with separately permissioned reveal, network cases with live-case deduplication, creator 7/30/90/lifetime assessments, and editable Signup/Network/Creator rule categories. The fraud subdomain route allowlist owns both `networks` and `creator-fraud`; a focused guardrail prevents the middleware from redirecting either clean route to the apex 404. Signup completion queues affected networks; a service worker processes scans, reconciles accounts every 15 minutes, and performs a daily creator scan. MAIN remains read-only; the antifraud service owns snapshots, jobs, scores, exact-value isolation, creator assessments, and cases. Service TypeScript plus 50 tests, dashboard TypeScript, zero-warning ESLint, 200 guardrails, diff check, and the production build pass. Production migration `008_account_networks_creator_fraud.sql` was applied by Railway deployment `0c43ada3-f55b-467a-b2a3-42c7e1f0fcff`, which reached `SUCCESS`; health, seeded analysis rules (5 Network + 10 Creator), network search, creator-fraud, and legacy scoring all return HTTP 200 with no new service errors or HTTP 500s. The optional read-mirror indexes remain recommendations only.

- **2026-07-28 Fiat jurisdiction policy activated:** backend PR #470 reached `main` through release PR #474, enabling non-US subdivision enforcement. The audited production admin action enabled card deposits globally, created the four Ukraine subdivision rows, preserved the exact 33 required `credit_card` exclusions, and reloaded the PROD country-restriction cache. A read-only mirror check confirms all 33 rows exist and are card-locked with none missing. Full-site geo blocking remains off; the policy restricts card deposits without blocking site access. `/fiat` and `/system/geo-blocking` no longer show the stale awaiting-deployment warning.

- **2026-07-27 Antifraud KYC workspace:** `fraud.packydash.com/kyc` is a full-width KYC Home with its own sidebar section, internal verification-cycle controls, Sumsub evidence, safe configuration-presence reporting, account search/status filters, and owner/admin review actions. The Fraud host owns the clean `/kyc` route; raw provider payloads and secrets stay hidden.

- **2026-07-27 Keno full-decimal odds:** `/keno` now expands very small draw probabilities into ordinary percentage notation, so `1.18e-7%` renders as `0.000000118%`.

- **2026-07-27 admin-dashboard production scope:** Global Codex and repo-local rules give every agent standing permission to work on and immediately ship every deployable unit stored in `PackyGG/pokewin-admin`, including its sub-apps, subdomains, API/serverless/edge functions, and repo-contained services such as `antifraud-monitor`. The permission includes full task-scoped production operation of the Antifraud stack, its Redis, the Admin DB, and the Antifraud DB, including reviewed migrations, service configuration, and required data work after exact-target/recovery verification. The MAIN game/customer DB and separate repositories remain protected.

- **2026-07-27 Dashboard Keno KPI:** the live dashboard KPI strip is four equal boxes at wide viewports: Wager, Deposits / Withdrawals, Crypto Fee, and Keno. The Keno card uses the same independently selectable Today / 24h window payload and shows settled customer wager, player payouts, house profit (`wager - payouts`), realized edge (`profit / wager`), games, and players. Staff, creators, and the admin blacklist are excluded. The production read is cached for 60 seconds, fails independently from the other KPI boxes, and uses `idx_keno_games_user_id_created_at` + `user_pkey` (read-only `EXPLAIN ANALYZE`: 0.428 ms).

- **2026-07-27 Quality Gates reliability fix:** GitHub Actions had failed every completed push since the workflow was introduced because the quoted `scripts/__fixtures__/*.test.ts` wildcard reached `tsx` as a literal path on Ubuntu. `scripts/run-guardrails.mjs` now discovers the fixture files and passes them directly to Node's test runner through `tsx`, independent of the host shell; both guardrail and parity scripts use it. The workflow also lets an active gate finish so rapid pushes no longer mark healthy builds as failed during cancellation. All guardrails, TypeScript, full ESLint, and the production build pass locally with the CI placeholder environment.

- **2026-07-27 Antifraud live monitor capacity recovery:** Railway evidence isolated the monitor outage to rapid `/v1/live` capacity closes followed by a `/v1/ws/tickets` retry storm. Ticket throttling is keyed by the authenticated staff actor instead of shared Vercel egress IP, safe Fastify `4xx` statuses are preserved, the legitimate per-actor WebSocket allowance is 8, and capacity retries wait at least 15 seconds. All three dashboard stream clients now remain subscribed after temporary terminal capacity/rate-limit frames instead of becoming permanently unavailable until reload. Monitor checks pass 40/40; all 199 dashboard guardrails, TypeScript, full ESLint, and the production build pass.

- **2026-07-27 Antifraud live-activity connection incident:** production logs exposed an upstream WebSocket 403. Normal backend REST requests included the configured `x-bypass-secret`, but `/api/packy-live` omitted it during the WebSocket upgrade. Backend configuration now resolves the bypass header once and shares it with both transports, with a focused guardrail locking the WebSocket path. The earlier truthfulness fix remains: the bridge emits distinct `upstream-open`/`upstream-error` events, closes failed responses, retries every 10 seconds, releases cancelled-stream capacity, and shows “Live” only after the upstream socket opens. TypeScript, ESLint, all 180 guardrails, the focused transport fixture, and the production build pass.

- **2026-07-26 Pack Builder EV auto-pricing:** Pack Builder calculates the pack sticker price automatically from the current pool EV and the configured target-edge curve. A custom 1%–50% target-edge override recalculates the price immediately, while an explicit manual-price switch remains available. New pools seed from equal-weight EV until shaped odds exist; then the exact live odds drive the price. Focused pricing tests, TypeScript, full ESLint, and the production build pass.

- **2026-07-26 Fiat operations workspace:** Overview now includes `/fiat` with active-tab-only Overview, Configuration, Payments, Access & holds, and Webhooks views. It reads the selected MAIN environment and can update only existing reviewed fiat config keys; missing money-control keys are never created from the UI. Creator Rewards and Notifications now live under Players. Read-only verification confirms production has a $500 per-card-deposit maximum and no lifetime withdrawal-hold threshold, while dev has the same $500 maximum plus the $100 lifetime completed-deposit hold threshold. TypeScript, ESLint, 173 guardrails, production build, and read-only prod/dev query plans pass.

- **2026-07-27 Admin Users organization:** `/admin-users` no longer relies on an invisible sort inside one “All Admins” list. Gallery and row views now render explicit non-empty Owners, Admins, Marketing, Pack Builders, Support, and Other staff sections with counts. Multi-role staff are grouped by their highest-ranked role. Focused tests, ESLint, TypeScript, all 185 guardrails, and the production build pass.

- **2026-07-26 monitor case timestamp cleanup:** `/antifraud/monitor/cases/[id]` now shows each monitor window's exact start date/time instead of a relative age, matching the exact end timestamp. The redundant PageHero back control is removed. TypeScript and full ESLint pass.

- **2026-07-26 Keno source-of-truth paytables and configuration:** Content → Keno mirrors all 30 compile-time backend payout curves from `backend/src/utils/keno.ts`; it no longer infers configured multipliers from sparse settled games. Odds & Chances always shows every Low/Medium/High multiplier for 1–10 picks, exact hypergeometric probability, configured RTP/house edge, return classification, and maximum win. Historical `keno_games` rows only verify observed cells and flag drift; DB failure no longer hides the static paytable. Configuration visibly owns all three active settings formerly on System / Security: withdrawal-requirement, leaderboard, and rakeback Keno weights, including their exact backend keys; the retired Shards weight remains hidden. It also names the real `0–39` backend position contract (all 7,117 inspected stored selected/drawn values stay in that range). `scripts/__fixtures__/keno-payouts.test.ts` locks completeness, backend anchor values, probability totals, and the ≈92.5% RTP profile. The production backend deliberately does not register `/v1/keno/multipliers`, so admin uses a tested compile-time mirror that must move with backend payout changes.

- **2026-07-26 Pack Builder scope + Shards retirement:** `pack_creator` is displayed as Pack Builder and single-role holders may use Pack Studio plus only `/packs`, `/cards`, and `/sets` in the normal Content section; middleware redirects every other dashboard/webapp route. Shards creation, editing, filters, balances, and catalog results are removed, while legacy shard rows remain untouched in the read-only MAIN DB. The canonical role and migration contain exactly 19 Packs/Cards/Sets tokens with no Upgrader or Shards access.

- **2026-07-26 Pack Builder owner approval gate:** `pack_creator` staff can submit inactive or live pack builds, but every request lands in the ADMIN `pack_creation_requests` queue and does not touch MAIN. The one-click, no-2FA **System → New Packs** review page now lives only in the dedicated Packs workspace, is shown only to owners, and enforces the DB-fresh owner gate on direct URLs. Approval revalidates current card prices/odds and then creates the pack, while decline creates nothing. Direct live activation remains blocked for non-owner `pack_creator` users. The focused ownership/location contract, all 180 guardrails, TypeScript, full ESLint, and the production build pass.

- **2026-07-26 antifraud signup network risk:** `/antifraud/signups` uses individually separated account cards; the score bar sits with identity and location, while point signals are capped to three compact tags plus an overflow count. It shows the green-to-red 0/40/80/120 score scale and explicit VPN, proxy, Tor, compromised-IP, scraper, and hosting badges from Fingerprint and proxycheck evidence. The live monitor carries the same shared display. Positive proxycheck v3 detection booleans always contribute 25 points for VPN/hosting/scraper/anonymous or 55 for proxy/Tor/compromised, with risk-score points additive; cached raw responses are re-parsed immediately. Signup cursor reads normalize MAIN microseconds to JavaScript's millisecond precision, preventing one signup from being reassessed and broadcast every poll tick. Monitor service check passes 35 tests; dashboard TypeScript, lint, and production build pass.

- **2026-07-26 cross-app PostgreSQL incident browser sweep:** authenticated production browser coverage completed across the apex admin app, Creator Hub, Pack Studio, Antifraud, and Marketing, including representative user, pack, transaction, and creator detail pages. Creator roster, profitability, socials, deals/sessions, and leaderboard reads use read-only MAIN PostgreSQL fallbacks while mutations remain backend-owned. The final release adds the remaining creator-detail leaderboard fallback, Upgrader outputs, announcements, and the Antifraud `/settings/api` pre-RSC redirect. Release gates pass: 169 guardrails, TypeScript, focused zero-warning ESLint, diff check, and production build.

- **2026-07-26 antifraud WebSocket lifecycle follow-up (`codex/antifraud-ws-handler`, verified for `main`):** live frames are appended to the Redis replay stream and published atomically, so clients cannot receive an id-less event that cannot be resumed. Readiness drops during either Redis connection outage; ticket reservation verifies `SET NX`; socket send/ping/error paths terminate safely; replay completion cannot reopen a closing upstream socket; and the shared SSE handler correctly distinguishes a previously healthy connection from a cold refusal. The redundant per-instance SSE tab counter was removed after it falsely terminally rejected a valid connection; the monitor WebSocket actor cap remains the authoritative limit and recovers through heartbeat/backoff. Monitor service check passes 38 tests; dashboard TypeScript, full ESLint, 170 guardrails, and production build pass.

- **2026-07-26 creator/backend PostgreSQL incident hardening:** production logs showed backend 500s on creator roster/session reads alongside direct MAIN pool-acquisition timeouts on dashboard, creator, leaderboard, and user routes. Production admin has `DATABASE_URL` but no `DATABASE_URL_POOLED`; MAIN allows 100 total connections. Creator Hub and legacy creator lists share an Upstash roster cache with a six-hour last-known-good value and single-flight refreshes. When the production backend roster still returns 500, the shared roster loader now falls back read-only to the canonical MAIN `user`, `creator_deals`, and `creator_stream_sessions` tables using the same role/current-deal/session/count contract, so the page no longer blanks on backend deployment failure; mutations remain backend-owned. Idempotent backend GETs retry one transient 500/502/504 or network failure, while mutations remain single-attempt. MAIN pool acquisition waits 35s, longer than its 30s statement backstop. Admin deployments `dpl_BVSur2xNm1SACEh8curv7rztEGiL` and `dpl_FFjGKYMHBfGHshS99mQiSwKaRKez` reached READY. TypeScript, zero-warning ESLint, 164 guardrails, diff check, and the production build pass; browser tests remain prohibited.

- **2026-07-26 antifraud webapp/backend audit completion:** recovered every unfinished lane from the Claude session named `Antifraud webapp/backend audit` and closed the adversarial follow-up. The dashboard now has a single resumable live transport, snapshot/completion ordering protection, complete review/case decision flows, retry-safe ADMIN audit mirroring, atomic manual review opens, indexed prefix search, deployed-runtime settings status, and the completed API/signups/overview surfaces. The monitor service now atomically persists ingestion/cursors, dead-letters poison signups without blocking siblings, caches successful enrichment across retries, fairly drains per-session activity, performs atomic session completion, reports leadership correctly, and binds decision/rule idempotency keys to the exact request. ADMIN migrations `20260726_antifraud_reviews_keyset_index.sql`, `20260726_antifraud_audit_idempotency.sql`, and `20260726_antifraud_review_search_indexes.sql` were applied and the snapshot refreshed. MAIN remains untouched; its two optional covering indexes are recommendations only. Railway preflight confirmed `SOURCE_DATABASE_URL` still resolves to the configured MAIN endpoint, so the safety guard refused the mirror-only index script; a real mirror URL is still required before that optional index set can be applied. The backend-monitor source PR #3 was squash-merged. Combined gates pass: monitor service TypeScript/build/32 tests, dashboard TypeScript/ESLint/build, 161 guardrails, and diff check.

- **2026-07-26 post-ClickHouse query-audit fixes:** the 110-finding Claude audit was recovered and revalidated against current `origin/main`. This batch covers all 3 critical and 14 high audit entries, plus 21 medium and 7 low correctness/runtime entries. Fixes include canonical wager/payout legs, battle/refund/voucher accounting, customer-scope consistency, UUID/text binding, env-safe caches, bounded lifetime reads, affiliate/deposit/race money and time semantics, and transaction-local read-only MAIN query timeouts. The remaining 11 medium and 54 low entries are the broader performance/bounds, timezone, dead-code, ranking, and series tail; do not treat the whole 110 as closed. No database writes or browser tests were performed. All 158 guardrails, focused audit fixtures, full TypeScript, full ESLint, diff check, and the production build pass.

- **2026-07-26 Staff notifications restored:** the shared header notification bell is active across Admin, Creator Hub, Pack Studio, and Anti-Fraud. `/system/staff-notifications` is restored as an always-visible System navigation page for authenticated staff, with inbox preferences, channel integrations, custom broadcasts, Anti-Fraud review/ingest alerts, and the idempotent `20260726_restore_staff_notifications.sql` ADMIN migration.

- **2026-07-26 antifraud poller hardening:** the one-second source poller now uses a PostgreSQL advisory leader lock so only one replica performs enrichment, drains signup backlog in bounded batches, and tracks each active monitor with a durable `(occurred_at, source, source_ref)` cursor instead of rereading its full three-minute window every tick. `/health`, protected `/ready`, and `/v1/operations/poller` expose sanitized freshness/failure/backlog telemetry. Migration `003_poller_hardening.sql` creates the cursor table. Production verification also exposed and fixed a latent PostgreSQL ambiguity in activity score arithmetic by explicitly typing both score parameters. Railway deployment `c06fa150-772b-4309-9de5-23d741df8852` is healthy: leader active, 16 ms tick, zero failures/skips/backlog, and no error logs. Service TypeScript, 15 tests, and production build pass.

- **2026-07-26 admin live-update rollback:** the deposit/card-payment/withdrawal and user Overview/Gaming live-refresh experiment is fully removed. The 3-second MAIN snapshot watcher, `admin.activity` protocol/filtering/cache invalidation, `LiveDataRefresh`, and its page mounts are gone. `/api/packy-live` is chat-only again. User detail keeps its prior 60-second `AutoRefresh` and manual transaction refresh. TypeScript, focused ESLint, guardrails, and the production build pass.

- **2026-07-26 direct Drizzle array-write hardening:** a TypeScript-AST audit found 13 direct JavaScript-array interpolations that the regex array fixture missed because they were ADMIN array-column assignments/VALUES rather than `ANY`/`ALL` reads. Admin role/page/permission arrays, custom-role capabilities, recovery codes, and the polymorphic crypto-withdrawal lock value now use `sql.param`; the same AST scan is empty, focused ESLint and full TypeScript pass. The CI fixture still needs the equivalent type-aware direct-assignment coverage.

- **2026-07-26 PostgreSQL cascade follow-up:** the dashboard's 16-leg aggregate batch is limited to two concurrent MAIN reads so the three-connection pool retains capacity for other routes. Canonical metrics and dashboard reads no longer fan out to the creators backend for session windows that cannot affect a scope which already excludes every creator. Query fallbacks remain no-throw even when console/Sentry/error getters fail, and the race claim-window timestamp uses the canonical PostgreSQL decoder. Added focused concurrency, creator-scope, and containment fixtures; full guardrails, TypeScript, ESLint, build, and post-deploy monitoring are the release gates.

- **2026-07-26 production crash hardening:** `/admin-users` null permission arrays are backfilled and constrained; PostgreSQL array parameters, raw timestamp decoding, and nullable permission reads are guarded. Root/client error boundaries contain route failures. All render-time ADMIN writes were removed or deferred until after rendering, sticky support/pack-creator permissions now materialize without page-load mutations, and the reviewed ADMIN backfill migration was applied. Strict fixtures reject bare SQL arrays, unsafe raw timestamp contracts, and page/layout DB writes. Validation passed 105 guardrails, TypeScript, ESLint, and the production build. Production deployment `dpl_3sVsoZ7qRMzJrWFoYMCKsuXpwCNV` reached READY with no post-release warning/error logs.

- **2026-07-26 PostgreSQL runtime guardrails:** every raw `execute` / `queryRows` / `queryMainRows` timestamp contract now admits the driver’s `Date | string` shape and normalizes before Date methods. A TypeScript-AST fixture rejects future Date-only raw timestamp contracts, including local named row types. All fixture tests run before every production build and in the new push/PR quality workflow alongside TypeScript, ESLint, and the production build. Recoverable `safeQuery` PostgreSQL failures also emit sanitized Sentry events with bounded area/kind/duration context and no SQL, parameters, request data, or user identifiers.

- **2026-07-26 Staff system scope:** Staff Hub, profiles, presence, points, and quizzes remain removed. The shared inbox/bell, notification channels, Anti-Fraud alert delivery, and `/system/staff-notifications` management page were restored.

- **2026-07-26 PostgreSQL timestamp hotfix:** production deployment `dpl_C5sJKsffgjF2LZDT93iepqnQ4YPB` exposed raw Drizzle timestamp strings where migrated code expected `Date` objects. Vercel confirmed `/admin-users` digest `2875875881` (`last_login.toISOString`) plus the isolated header-rain failure (`starts_at.toISOString`). Both exact failures and every audited raw-row Date-method path now normalize `Date | string` before use. TypeScript, full ESLint, and the Next 15.5.22 production build pass.

- **2026-07-26 antifraud deposit split:** signup monitoring no longer ingests rain joins/wins. Completed ledger deposits are now classified as `fiat_deposit` only when linked through `fiat_deposit_intents.completed_ledger_id`, or `crypto_deposit` only with crypto settlement evidence; anything else fails closed as `deposit_unclassified`. Fiat/unclassified adds 20 risk points for an already-suspicious monitored signup, while irreversible crypto reduces risk by 20. The replica-only index list now covers the fiat ledger join, and migration `002_split_deposit_risk.sql` upgrades existing behavior-flow blockers.


- **2026-07-26 compact risk-scoring page:** the manager-only scoring catalogue moved from `/antifraud/settings/points` to `/antifraud/points` because it is an operational reference, not a workspace setting. The oversized hero, notice, KPI tiles and per-signal cards were replaced by a slim full-width header, one summary strip, compact severity legend, and dense grouped rows for all live weights and behavior flows.
- **2026-07-26 Vercel build recovery:** commit `990e6dc1` accidentally included layout fragments from an uncommitted Prisma-to-Drizzle migration. The complete PostgreSQL/Drizzle migration is now included, so the previously missing imports and schema files are present in the production tree.

- **2026-07-26 antifraud boundary hardening:** the dedicated monitor API now has separate read/admin tokens, exact production origins, Helmet/no-store headers, global and mutation rate limits, bounded bodies, protected readiness, one-time WebSocket tickets, origin enforcement, authoritative per-actor connection caps, ping/pong eviction, and slow-client cutoff. Dashboard snapshot/SSE bridges reject cross-site requests and rate-limit starts without maintaining a second unreliable serverless connection counter.

- **2026-07-26 PostgreSQL error cleanup:** active runtime no longer carries Prisma P20xx compatibility branches or stale Prisma schema/tooling guidance. `src/lib/postgres-errors.ts` now extracts SQLSTATEs through Drizzle `cause` wrappers, rejects retired Prisma codes, and centralizes unique/missing-relation handling across auth, roles, creator rewards, staff, profile, cards, layouts, and migration-lag fallbacks. Active-source P20xx scan is empty; focused ESLint, `tsc --noEmit`, six PostgreSQL/boundary fixtures, and diff check pass.

- **2026-07-26 Drizzle correctness hardening:** positional `$n` binding now ignores quoted strings, comments, and dollar-quoted bodies; creator payouts, balance adjustments, vault moves, manual withdrawals, and balance-neutral removal ledgers keep authoritative Decimal arithmetic in PostgreSQL. Creator-reward approvals use an ADMIN compare-and-set, MAIN-ledger recovery, and a per-claim PostgreSQL advisory lock so concurrent/crash retries cannot pay twice; rejection now compare-and-sets only an unreserved pending claim and cannot overtake an approval already moving money. Quiz answers and role rematerialization are batched; role writes reject concurrent per-user permission edits instead of overwriting them. Focused ESLint and `tsc --noEmit` pass.

- **2026-07-26 post-Drizzle delivery/cache audit:** the dashboard's shared today-window payload is React-deduped across its KPI and P&L Suspense branches, and the Creator Hub route loader matches the current four-tool, four-week, and two-chart layout. Focused ESLint and diff checks pass. Full `tsc` was blocked at that snapshot by concurrent antifraud/staff file moves and stale `.next` route types, not these files.

- **2026-07-26 dedicated antifraud monitor:** new standalone service under `services/antifraud-monitor` reads Packy through a read-only source connection, stores cases/events/rules/provider evidence in the dedicated Antifraud Postgres, and streams live events through Redis + ticketed WebSockets. Signup scoring combines existing fingerprint/IP/geo data with Fingerprint Pro Plus and proxycheck v3; suspicious signups open configurable 3-minute monitor sessions. HTTP includes `/v1/top-rain` (authoritative completed `rain_win` ledger payouts). Admin surface `/antifraud/monitor` uses authenticated same-origin snapshot/SSE bridges; provider keys and service token stay server-side. Railway production has a dedicated service + Redis; source switches to the mirror by replacing `SOURCE_DATABASE_URL`, then apply `migrations/source-mirror-indexes.sql` to the mirror only. Matched rules now send sleek Discord webhook alerts with an Antifraud link button and an exact allowlist: three support accounts on every alert, four additional accounts only when a future trigger explicitly sets `urgent`. The Railway secret is configured without a redeploy; the service check passes 7 tests and a live standard-priority test webhook returned 200. No Discord bot integration and no crypto monitoring.

- **2026-07-26 antifraud API cleanup:** the manager-only catalog now lives at `/antifraud/api`, lists only the antifraud service's external HTTP endpoint (`GET /v1/top-rain?limit=`), and excludes health/readiness, monitor internals, dashboard bridges, ticketing, and WebSockets. The old `/antifraud/settings/api` route redirects to the canonical URL. Focused ESLint and `tsc --noEmit` pass; unauthenticated browser verification correctly redirects to `/login`.

- **2026-07-26 antifraud Discord settings:** the manager-only `/antifraud/settings` surface is now named **Settings** (including sidebar and metadata) and uses active-tab-only General/Discord navigation. The Discord tab documents the production `PackyGG Fraud` webhook contract without exposing its secret: blurple/red styling, monitor link button, automatic rule-match delivery, fixed support/urgent mention allowlists, and the still-undefined urgent trigger. Focused ESLint and `tsc --noEmit` pass; desktop and 390px browser fixtures render without horizontal overflow. The authenticated route correctly redirects an unsigned local browser to `/login`.

- **2026-07-26 subdomain URL cleanup (`d0407b34`):** Pack Studio and Antifraud links now resolve directly to prefix-free subdomain URLs. Middleware canonicalizes old `/pack-studio`, `/antifraud`, and `/marketing` bookmarks by moving them to the owning host and stripping the obsolete prefix. Shared `HostLink` covers direct links, PageHero back links, portal cards, notification links, router pushes, and Pack Studio transport requests. Clean-worktree tsc, lint, and build passed.


- **2026-07-26 dashboard side-widget removal:** the admin shell no longer mounts the Live money, Recent activity, or docked Chat/Mutes widgets. Deleted their UI wrappers, live-feed server actions/API route, dashboard live-query module, and the mini-user popup path that only the Live widget used. Pack Studio and Antifraud no longer mount empty rail scaffolding. Creator Hub keeps its independent Alerts dock; shared rail state is reduced to Alerts only. Targeted ESLint passes. Full `tsc` is currently blocked by two pre-existing Drizzle migration errors in `src/app/(admin)/users/[id]/actions.ts` (lines 3049 and 3602). Local browser verification reached `/login` but could not inspect the authenticated dashboard without credentials.


- **2026-07-26 PostgreSQL/Drizzle migration:** runtime database access is PostgreSQL-only through typed Drizzle clients and bound SQL; ClickHouse and Prisma clients, packages, schemas, flags, fallbacks, sync/health paths, environment variables, and dead query trees are removed. Main/Admin Drizzle introspection snapshots and native seed/SQL tooling are tracked. Query passes batch N+1 reads, cap pagination, reduce over-fetching, preserve financial row locks/transactions, and cap pools. A read-only catalog/EXPLAIN pass removed two premature fiat-intent indexes (39 live rows), completed the sold-inventory covering shape, bound creator-profitability blacklist arrays, collapsed pack-creator capability backfill from one write per page to one set-based update, and fixed enum casts that prevented the live partial deposit index serving the user deposit picker (planner cost 11,493 plus sort to 128 without sort). Live read-only validation passed 20 concurrent parameterized reads against each DB, 76 fixtures, 51 ADMIN limit checks, TypeScript, scoped/full-source ESLint, and production build. Browser/Playwright verification is intentionally prohibited by `CLAUDE.md`. The separate permission-parity probe found pre-existing ADMIN system-role seed drift (`support` lacks `/dashboard`; `pack_creator` retains removed `/rewards/shards`) while all per-user effective access still reconciles; owner-reviewed ADMIN SQL is required before claiming seed parity. New MAIN covering/partial/expression recommendations remain owner-applied because MAIN is strictly read-only.

- **2026-07-26 Drizzle schema/tooling fidelity:** live read-only catalog comparison confirms MAIN 94 tables/1,159 columns/45 enums/130 indexes/139 FKs and ADMIN 80/810/7/137/49 match the checked-in snapshots. The pull workflow now normalizes drizzle-kit's empty-array and unsupported `bytea`/`oid` output, restores the representable cyclic FK metadata, and fails closed for an unconfigured DEV DB. Runtime ADMIN DDL/self-heal paths were removed; the two reviewed idempotent replacement migrations were applied live successfully. Admin migration/seed tooling now rejects MAIN URLs, migration paths outside `drizzle/admin/migrations`, BOM files, and transaction escape statements. TypeScript, scoped ESLint, boundary tests, normalizer idempotency, diff checks, and the production build pass.
- **2026-07-26 SQL safety follow-up:** the legacy `$n` compatibility parser is isolated and regression-tested for repeated/multi-digit binds, currency literals, comments, dollar quotes, missing/unused values, and malformed quoted regions. Dynamic feature-lock identifiers use Drizzle identifier quoting. Remote E2E MAIN writes can no longer be enabled by a flag, the unguarded legacy MAIN weight-migration script and unused unbounded session query were removed, and user-detail deposit-address reads are capped at 50.

- **HEAD:** `origin/main + admin-users visible grouping` · **Updated:** 2026-07-27 · **Active focus:** Admin Users role sections are verified and ready to publish. · **⛔ UI REDESIGN HALTED BY OWNER (2026-07-02): do not resume the broad Liquid Glass restyle.**

## ✅ Shipped — Creator Hub surface removal (`dbab8ac3`, 2026-07-23, owner request)

**Hub nav is now 6 entries: Dashboard · Creators · Tips & Sponsors · Socials Review · Profitability · ROI Calculator.** Deleted whole routes + every function that only served them: `/creator-hub/{changelog,sessions,leaderboards,wager-abusers}` and the `creators/[id]` **Forecast** + **Cohorts & LTV** tabs (`forecast-tab|chart|data`, `cohorts-ltv-tab`, `cohorts-chart`, `cohorts-data`, `all-sessions-table|data`, `force-end-session-dialog`, `live-leaderboards` + ranklist + time-left, hub changelog feed + period selector).

- **Leaderboards + Changelog still exist in the main admin app** (`/creators/leaderboards`, `/creators/changelog`) — the hub copies were ports. The creator-detail Leaderboards card + Previous-boards dialog now link to **`/creators/leaderboards[/:id]`**.
- Also removed: dead `revalidatePath("/creator-hub/{leaderboards,wager-abusers}")` in the admin leaderboard + user-tag actions; `ABUSER_HUB_TAGS` from `lib/queries/user-tags.ts` (wager-abusers was its only consumer — `getUsersWithTags` stays, `/vips` reaches it via `getUsersWithTag`); the deleted routes from `e2e/responsive/config.ts` + `e2e/tests/creator-hub.spec.ts` (leaderboard-detail test dropped, forecast-tab test retargeted to Risk).
- **Verified still needed, do NOT delete:** `getCreatorsChangelogEvents` (admin changelog), the leaderboard standings/claims panels + rankings/claims queries (admin), `getCreatorLeaderboardCost` + `getCreatorTipsSponsorCost` (admin deal-cost panel **and** the hub Overview tab), and the hub creator-cost/top-creators queries (dashboard). The hub's `fetchAllApprovedLeaderboards` was a local copy; admin keeps its own in `leaderboard-cost.ts`.
- Alert deep-links all target `/creator-hub/creators/...` — none pointed at a deleted route. Post-delete orphan sweep: **zero unreferenced files**.
- `session-parts.tsx` (extracted in `f162a853` for the two session tables) now has one consumer → moved under `creators/[id]/_components/`.

---

## ✅ Shipped — Creator Hub audit + cleanup (`f162a853`, 2026-07-23)

Full read-through of `src/app/(creator-hub)/**` + `src/lib/creator-hub/**` (143 files / 32k lines), then one commit: **+1,376 / −1,981**.

- **Dead code:** `creator-checklist.tsx` (superseded by `creator-checklist-dock.tsx`, 0 refs); **`src/lib/creator-hub/cache.ts` deleted** — the Kick/Twitter RapidAPI cache/TTL/throttle + brand-keyword layer outlived its removed integration; only the handle normalizer was consumed → kept as **`handles.ts`**, barrel trimmed. Also `getHubCreatorCostUsd` (`@deprecated`, 0 callers), `padHubChartSeries`, `hubChartTitleSuffix`, `bucketLabel`, `HubChartPoint`, and HubQuickTools' `isPlaceholder` branch (no tool has `href:"#"` any more).
- **Dedup:** the two session tables carried ~500 lines of byte-identical copy (the file doc literally said "some duplication is accepted here"). VOD editor / detail modal / status styles / `Datum` / `Detail*` / `CommunitySpend` / `LivePulse` / money+date+duration helpers → **`creator-hub/_components/session-parts.tsx`**. `sessions-table` 817→309, `all-sessions-table` 962→442. **Edit the shared file, not one surface.**
- **Streaming/robustness:** `leaderboards/[id]` was a 6-way un-timed fan-out blocking first paint → shell-first (only the bounded board read is critical-path); prize pool now goes through `formatCurrency` instead of raw `${…}` interpolation. `leaderboards` + `socials-review` rendered their filter/status controls INSIDE the Suspense boundary, so every switch replaced the control the user just clicked with a skeleton → controls hoisted to the shell, leaves share one `React.cache`'d fetch (no extra query). `profitability`'s **Active** tab (the default) lacked the hard try/catch Past Deals carries for digest `3304963582` → same isolation. `creators/[id]` used `RiskTabSkeleton` for every tab, pulling the 500-line risk tab into the page graph → `CreatorTabSkeleton`.
- **Checked, no change needed:** money is Decimal-safe via `toNumber` throughout; every creator-detail tab degrades via `safeQuery` (forecast is safe — all 4 legs wrapped inside `getForecastData`); `getCreatorLeaderboardWagerMap` IS `unstable_cache`d (the live-leaderboards wager pass is not the unbounded scan it looks like); the `UPPER(code)` functional index is already tracked in `prisma/recommended-indexes.sql`.
- Gates: `tsc` + `lint` + `npm run build` all green. No prod-DB access of any kind.

---

## 🚦 ACTIVE COORDINATION (2026-06-12) — read before touching files

**Multiple agents are working this repo in parallel RIGHT NOW. `main` = prod and is receiving pushes from isolated worktrees. Always `git pull --rebase` before pushing. Keep your file scope tight and listed here.**

**🎯 EDGE-PLAN REWORK ACCEPTANCE NUMBERS (owner-confirmed live from /insights hub, 2026-06-12 ~04:40):** Wager **$3,211,825.07** (Lifetime · real customers, via `getInsightsHubWager`) · Total P&L **+$44,925.74** (balance-sheet snapshot incl. unclaimed rakeback, via `getCostBreakdown` pnl). Any earlier "~+$120K realized P&L" hint is OBSOLETE — reconcile edge-plan-2 against the LIVE helper values; these are the current ballpark. The `edge-plan2-data-rework` workflow (worktree `_wt-edge-plan2-rework`) SUPERSEDES Workflow A below for all edge-plan-2 files; on rebase conflicts in edge-plan-2/** its version wins.

**Workflow A — Edge Plan 2.0 overhaul (running, worktree `_wt-edge-plan2-overhaul`).** OWNS (do not touch): `src/app/(admin)/insights/edge-plan-2/**`, `src/app/(admin)/insights/system-edge-plan/**`, `src/lib/queries/insights-rewards/raffle/**`, `src/app/responsive-fixture/edge-plan-2/**`, `e2e/tests/edge-plan-2.spec.ts`, `scripts/probe-edge-plan-recon.ts`.

**Workflow B — app-wide perf/reliability audit (SHIPPED its safe-fix batch from worktree `_wt-app-audit`; audit phase complete).** Audited everything read-only; implemented ONLY behavior-preserving fixes (queries/errors/loading mechanics). Shipped scope (gates + Playwright render-sweep verified): pagination clamps in `src/lib/queries/{transactions,audit,withdrawals}.ts`; dead-relation + SELECT-star cleanups in `withdrawals.ts`/`whitelist.ts`; topbar safeQuery timeout (`src/components/topbar-house-stats.tsx`); safeQuery/TileErrorFallback hardening across `src/app/(admin)/analytics/tab-*.tsx` (+ Overview Suspense split); `loadPrimary`/safeQuery wraps on `/battles`, `/sets`, `/vouchers`, `/promo-codes`; streamed+degraded `/audit` + transactions table sections; `after()` for `/my-profile` socials refresh; creator-hub route `error.tsx` ×4 + hub-overview globalStats timeout leg. Deferred (data-meaning — owner sign-off needed, see audit report in session log): dual-DB env in cached money aggregates (`house-kpis.ts` + 3 dashboard caches), gift-card status pagination, `users-sessions` lifetime scan, LTV `all` cap, creators signups/FTD exclusion asymmetry, DockedAlerts eager sync, hub-cohort cache split, rain tips pagination, promo-code true redemption count. It did NOT touch: hotspots, frozen files, in-flight files, `insights/**`.

**Owner's parallel sessions (live):** "Rendered more hooks on Creator Hub" → owns the (creator-hub) component hook fix. "First-visit TZ hydration app-wide" → owns `src/components/timezone-provider.tsx`, `src/app/(admin)/layout.tsx`, and any `suppressHydrationWarning` sweep (root-cause = TimezoneProvider first-visit re-render racing late-hydrating streamed legs; per-page suppress is a patch, provider/layout is the right layer). UI-performance chat → owns visual loading-UX (skeleton quality, pending states). Don't duplicate each other's class of change.

**FROZEN (recent verified fixes / money math — report findings, never edit):** `src/lib/queries/users-transactions.ts` (owner-only adjustments gate), `src/lib/balance-adjustment-categories.ts` (null-safe 3VL predicates), `src/lib/queries/_ledger-tx-types.ts` (live prod-enum filter), `src/lib/queries/pnl.ts` + `users-windowed-pnl.ts` (**NEVER add `upgrader_games.won_amount` into ledger balance-delta P&L** — upgrader wins are inventory items, already counted via inventoryChange; commit `ea5e97b8` did it and broke Daily P&L → reverted `231884f9`), `src/lib/users/owner-adjustments-visibility.ts`, `src/lib/creator-hub-access.ts`, `src/lib/queries/dashboard.ts` (pending owner-side commit).

**DB (2026-06-11, CLAUDE.md):** NEW live prod game DB in `.env` — strictly READ-ONLY for everyone (no DDL by anyone, ever; indexes are dead ends — perf is won in code). Old-DB facts (enums/tables/indexes) are unverified on the new DB; re-probe before relying.

**Recently shipped (don't re-audit deeply):** `/users` list + `/users/[id]` remakes, `/creator-hub/creators/[id]` remake, gaming-tab enum fix, page-size fix, 3VL adjustment fix, P&L revert. All verified on live prod data.
- **Note (2026-06-06):** local checkout was on branch `dev` (even with `origin/main`) with **no `node_modules` / `.env`**; ran `npm install` + `prisma generate` (both clients) to gate.
- **Cloud VM dev env:** merged **PR #48** — `AGENTS.md` § Cursor Cloud specific instructions on `main`; update script `npm install`. Local VM: Postgres 16 + `.env.local`; lint/tsc/build + Playwright auth PASS.
- **Deploy:** `main` → Vercel prod `pokewin-admin.vercel.app`
- **Route segment:** `src/app/(creator-hub)/creator-hub/` (sub-app with own layout + sidebar)

---


## ⚠️ GOTCHA — admin-schema columns must be `db push`ed BEFORE the deploy lands (2026-07-23)

**Symptom (live, /creator-rewards):** BOTH "Couldn't load the programs" and "Couldn't load the claim
requests" notices at once, with empty lists underneath.

**Cause:** a commit added columns to `creator_reward_programs` (`lossback_pct`, `min_deposit_usd`,
`vip_reward_usd`) and `creator_reward_claims` (`leg`, `ftd_deposit_usd`, `ftd_loss_usd`) — commits
`211d1654` / `08b7a385` — and deployed before the admin DB got its `db push`. Prisma `findMany`
selects EVERY scalar column in the model, so one missing column fails the WHOLE read (P2022). Both
page reads do a full-column findMany on those two tables → both degrade together, which is exactly
what the two notices are. `safeQueryOrNull` only returns its null sentinel on a THROW, never on an
empty result, so "both notices + empty lists" always means the queries threw, never "no rows yet".

**Rule:** an admin-schema column addition is only shipped once
`npx prisma db push --schema=prisma/admin/schema.prisma --config=prisma/admin/prisma.config.ts`
has run against the admin DB. Push the schema FIRST (additive columns are backward-compatible — the
older running build simply doesn't select them), then deploy the code. The reverse order takes the
affected pages down for the whole window in between. **Refreshing never clears this** — if a page
degrades and a refresh doesn't fix it, check for drift before looking anywhere else. `db push`
reports "already in sync" when there is none (verified 2026-07-23: the admin DB is fully in sync,
34/100 connections, both tables present and empty, equivalent raw SQL ~24 ms).

---

## Shipped (recent -- on main)

**2026-07-22 — /notifications: full announcement payload support (`c7aaf444`)**

The announcement API always accepted a structured payload next to title/body — the composer never
exposed it, so every broadcast shipped as bare text. Contract (mirrored 1:1 in the new shared
`src/lib/announcement-payload.ts`, verified against backend `src/schemas/websocket.ts`
`AnnouncementPayloadSchema`): `url` http(s) only, `image_url` **ImageKit-only**
(`https://ik.imagekit.io/scrkflpgw/…` — the frontend's next/image allowlist), `cta_label` ≤ 60 chars.
**Unknown keys are stripped by the backend**, so there is no `promo_code` key — a code has to live in
the body copy (flagged to the dev as a possible backend addition).

Composer (`create-announcement-dialog.tsx`, extracted out of `announcements-content.tsx`) now has four
templates: **Message / Pack / Page / Promo code**. Pack picker → fills the pack's ImageKit image +
`/games/packs/<slug>` link + button (all 268 imaged packs on prod are ImageKit-hosted; 14 image-less
ones are inactive — probed read-only). Page → preset public routes verified against the live frontend
nav (`nav-dropdown`/`mobile-bottom-nav`/`footer`), stored in `MAIN_SITE_PAGES` in
`src/lib/utils/main-site.ts` alongside a new `packUrl()`/`mainSiteUrl()`. Promo → picks a live code
(plaintext lives in `promo_codes.metadata.code`, the column itself is a hash) and composes the copy;
there is **no deep link to the wallet promo tab**, so the code in the body is what users copy.
Image field does a direct ImageKit upload (same `uploadImageClient` path as the pack/card forms) or
manual URL, with preview + non-ImageKit guard; live preview of the rendered notification; per-template
`type` tag (`pack_release` / `page_release` / `promo_code`, default `admin_announcement`) editable
under Advanced so the site can key an icon off it.

Lookups (`composer-actions.ts`) are MAIN-DB **read-only**, gated on `/notifications` page access +
`__can_manage_announcements`; the promo list additionally soft-gates on `/promo-codes` access →
otherwise `restricted: true` and manual code entry. `packs` (282 rows) / `promo_codes` (3 rows) are
tiny catalog tables — bounded `findMany`, same read shape as the existing /challenges + /rewards
pickers. List table now renders the payload (thumbnail, link, button label, type) and the create
action validates payload + title/body/type lengths and records the payload in the audit event.
Gates: tsc + lint + `npm run build` all green. No render check (owner rule).

**2026-07-22 — /security → Telegram Notifications section (`ab1bbf22`)**

New collapsible section: min-deposit `$` input + new-signup toggle, wired to the backend's
`GET/PUT /admin/telegram-notifications`. Built on the deposit-bonus-config trio pattern
(backend-api module + `-keys`/`-actions`/`-card`), prod-cached read, fault-isolated leg,
both site_config keys added to `movedKeys`. MAIN stays read-only — writes go through the
backend admin API only. Added `bell` to `SECURITY_SECTION_ICONS`. Gates: tsc + lint + build all green.
Backend shipped (#455/#456) and verified live on `/security`.

**2026-07-22 — Telegram Notifications: master switch + per-alert toggles (in `196087b6`)**

Card expanded from 2 knobs to full per-alert control: a master switch (off = nothing sent, card dims
to make the override visible) plus one toggle per alert — deposit confirmed/failed, withdrawal
requested/completed/failed, new signup — alongside the existing min-deposit `$` input. Backend side is
`telegram_notifications_enabled` + `telegram_notify_<kind>` keys, all defaulting to true, read per-call
so a toggle applies with no restart. Signup keeps its original
`telegram_signup_notifications_enabled` key so an already-saved value isn't orphaned.
Gates: tsc + lint + build all green.
**⚠️ These 4 telegram files were swept into `196087b6` ("feat(users): show the actual device ID…") by a
concurrent agent using `git add -A` — the code is correct and live, only the commit attribution is off.**

**2026-07-12 (part 26) — wave 16b + full audit fix sweep (4 commits: `82daed7b`, `84ec9009`, `2aa58667`, `a07f4c4e`)**

Owner: "literally fix all possible things, all options or scenarios i want it perfect!"

1. **`82daed7b` — applyPackEdit refusal-as-data fix:** same prod-masking pattern as `applyStagedPackEditAndRetune`/`applyPackRetune` (incident 2026-07-11). `applyPackEdit` renamed to `applyPackEditInner`; exported wrapper catches throws → returns `{ refusedMessage }`. `pushDraftToProd`/`pushAllDrafts` wrapped same way. Client `drafts-list.tsx` handles `refusedMessage` returns. `unstable_rethrow` added to `pushAllDraftsInner` catch (audit M6).

2. **`84ec9009` — wave 16b dead-card-removal rescue + full audit fixes:**
   - **Engine (`tag-guidance.ts`):** New Tier D in `computeCleanRescue` — after all other tiers fail, near-zero-weight cards (<0.1% share) are removed one at a time (cheapest first) and the reduced pool re-solves at the pack's OWN edge target. Owner-pinned rows never removed; `pinnedShares` indices remapped; `currentWeights` filtered; own budget (`CLEAN_RESCUE_DEAD_CARD_BUDGET=4`); degeneracy veto on reduced pool. `CleanRescue` gains `removedCardIds`. `buildCleanRescueSuggestion` now emits a `remove-dead-card` fallback chip for tier D (was: null when `edgeTargetOverride===null` — no visible path when auto-adopt fails; audit C1+H1).
   - **Workspace (`workspace.tsx`):** Auto-adopt effect: new `dead-card-removal` branch removes card(s) from staged pool + drops pins + pins proven price, then re-plans. Global per-pack adoption cap (`RESCUE_GLOBAL_CAP=6`) as backstop against unbounded oscillation across basis changes (audit H3); cleared on reset-to-live and push success (audit L1/L3 memory). Pending pct range validation: reject ≤0 or >100 (audit M5s). `applyPinRemedy`: basis staleness guard (audit H4) + toast on stale no-op (audit M2).
   - **Copy (`plan-copy.ts`):** `priceMoveSub` defaults to honest "still don't land clean" when `landedClean` is not explicitly `true` (audit L2s). `autoCleanAppliedToast`: new dead-card-removal branch.
   - **Plan panel (`plan-panel.tsx`):** `pushDisabledLabel` explicit infeasible case (audit M1) — "Infeasible — try a suggested fix above" instead of misleading "Fix the pool first" catch-all.
   - **Harness:** clean-rescue 20→25 (+5 tier D checks: removal, owner-pin skip, no-dead-cards skip, budget law, suggestion builder).
   - **Gates:** tsc 0 · lint 0 · build 0 · 13/13 suites green (364 checks).

3. **`a07f4c4e` — drafts house-POV colors + edge regression toast:** `WeightChangeTable` now receives `packPrice` and colors by house-POV (weight up on win card ≥ price = rose/bad; weight up on junk = emerald/good; vice versa) — was raw delta as neutral proxy (audit M3). `runPush` uses `toast.warning` when house edge decreased (audit M4).

**Adversarial audit findings DEFERRED (LOW priority, not blocking):**
- H2 (picker race — card added to wrong pack on rapid selection switch): would need capturing packId at `stagePoolEdit` call time and threading through picker; complex change, low real-world probability
- M5 (tier D multi-card removal combinations): current single-card-only is by design (owner removed one card by hand); pairs would need bounded budget expansion
- L3s (deriveStatus returns "infeasible" for out-of-scope): workspace already handles via separate `outOfScope` check; adding new status would require changing `WorkspaceStatus` type + all consumers
- L4s (isPushEnabled allows push on dirty plan under staged pin): documented escape hatch by design

**Open retune items (unchanged):** wave 14 concrete add-card pick; dust cap + floor comfort mechanic; degenerate-carrier copy calibration.

**Full pre-2026-06-16 feature-by-feature shipped log (legacy analytics milestones M1-M4, owner/role
rework, dropdown onSelect fix, etc. -- all still true/live, just no longer "recent") moved to
`AGENT_HANDOFF_ARCHIVE.md` section Shipped-feature detail log.** Recent work is narrated in CURRENT STATE
above and the session log in the archive (parts 1-25, 2026-06-16 to 2026-07-12) -- the most recent 2-3
parts there are the actual "what shipped lately" answer; older parts are historical record.

## 🟡 In-flight

**✅ CLOSED 2026-07-09 (stale entries — both verified LANDED on `main` long ago):** the two items below were flagged "LOCAL/uncommitted" from a loose checkout, but all 8 admin files are tracked on `main`, wired into `/security` via `security-page-sections.tsx` (imports `RewardExpiryCard` + `ShardWagerWeightsCard`), and were even refactored since (`5a7cf69c` optimistic in-place save). Durable finds that still apply: backend `site-config.service.ts` has dead SINGULAR `shard_source_weight_*` DEFAULTS (live keys are PLURAL `shards_source_weight_*` — harmless, could be dropped backend-side); prisma generate/build needs Node ≥22; frontend UX gap remains (race/leaderboard user UIs render expired prizes as claimable until claim 4xxs — packygg-frontend work, not started). Original entries kept below for context.

**Reward Expiry config (2026-06-10, LOCAL/uncommitted, backend + admin) — PARTIAL:** owner request — per-reward-type claim windows ("rakeback expires in 3 days, race winnings in 7"). Reward types: rakeback / race / leaderboards / reloads (= balance rewards: Reload is a balance-reward category alongside Bonus/Giveaway/Deposit-fix/Lossback).
- **Backend (`packygg-backend`):** rakeback ALREADY had per-type expiry (`rakeback_config.expiration_days`, enforced) — race prizes and leaderboard prizes explicitly never expired, balance rewards never expired. Added: 3 site_config keys `reward_expiry_{race,leaderboard,balance}_days` (DEFAULTS `'0'` = never, preserving current behavior) + getters; claim-time enforcement in `race.service.ts claimRacePrize` (anchor `period.ends_at`, error `RACE_PRIZE_EXPIRED`), `affiliate-leaderboard.service.ts claimPrize` (anchor `end_date`, ForbiddenError), `claimBalanceReward.ts` (anchor `granted_at`, new `ERROR_CODES.BALANCE_REWARD_EXPIRED`); new repo method `rakeback.updateExpirationDays`; new admin route `/admin/reward-expiry` GET/PUT (`{ rakeback: {daily,weekly,monthly}_days, race_days, leaderboard_days, balance_days }`, days int 0..3650, rakeback writes through to `rakeback_config`, rest to site_config) registered in `admin/index.ts`.
- **Admin UI:** `/security` "Reward Expiry" card (Hourglass icon, below Funding-Source): `reward-expiry-card.tsx` (4 groups: Rakeback daily/weekly/monthly · Race winnings · Leaderboards · Reloads & balance rewards; whole-day inputs, "never expires"/"expires after N days" hint, changed-fields-only) + `-actions.ts` (requirePageAccess+requireAdmin, audit `reward_expiry_updated` old→new) + `-keys.ts` (3 site_config keys → movedKeys) + `src/lib/backend-api/reward-expiry.ts`.
- **Known UX gap (frontend follow-up):** enforcement is claim-time only — `packygg-frontend` race history / leaderboard pages still render expired prizes as claimable (claim then 4xxs with the expiry error). Rakeback UI already shows `is_expired`. Surfacing expiry in race/leaderboard/reward user UIs = packygg-frontend work, not started.
- **Verify:** backend `tsc` — 0 errors in touched files (21 pre-existing unrelated, missing-dep typings; backend `npm run lint` broken repo-wide: eslintrc-format config + ESLint 9). Admin: `tsc` exit 0 · lint clean · `npm run build` exit 0 (Node 22). NO live click-through (no backend env; card renders degraded state). NOT committed (loose checkout, see below).

**Shard wager-weights admin UI (2026-06-10, LOCAL/uncommitted) — PARTIAL:** two `/security` cards added to expose the already-shipped backend shard knobs (backend on `packy-backend`: per-game `shard-wager-weights.ts` route + `shards` as the 4th `source-wager-weights.ts` destination, both already wired into all four wager callers).
- **NEW per-game "Shard Wager Weights" card** (Gem icon, placed before Funding-Source): `shard-wager-weights-card.tsx` + `-actions.ts` (requirePageAccess+requireAdmin, audit `shard_wager_weights_updated`, backend PUT) + `-keys.ts` (`shard_wager_weight_{packs,battles,upgrader}_bps`) + `src/lib/backend-api/shard-wager-weights.ts` (GET/PUT `/admin/shard-wager-weights`). Mirrors the leaderboard-weights card exactly.
- **Funding-Source card extended** with a 4th `shards` destination (withdrawal/rakeback/leaderboard/**shards**): added to lib types (`shards?` optional → graceful per-destination degrade), card `DESTINATIONS`, action Zod schema + refine, and the `movedKeys` filter.
- **⚠️ Backend key-naming finding (NOT changed):** the funding-source `shards` keys persisted to `site_config` are PLURAL `shards_source_weight_*` (PUT/GET build the key from the `'shards'` destination string). The `DEFAULTS` map in `packy-backend` `site-config.service.ts` has SINGULAR `shard_source_weight_*` entries → **dead** (never read; harmless, all = 10000 via the hardcoded fallback). Admin `movedKeys` uses the live PLURAL keys. Backend could drop the dead singular DEFAULTS entries; no functional impact.
- **Env finding:** `prisma generate` / `npm run build` need **Node ≥22** here (Prisma 7.5.0 WASM needs `externref`; Node 16 → `CompileError`, Node 20.18 → `ERR_REQUIRE_ESM`). Built green on `v22.14.0`.
- **Verify:** `tsc --noEmit` exit 0 · `lint` clean · `npm run build` exit 0 (Node 22). **NO live logged-in click-through** (no backend API env here → cards render the degraded "awaiting backend deploy" state). Recommend a logged-in pass on a deploy where the backend shard routes are reachable.
- **NOT committed/pushed:** this `PackyGG/pokewin-admin` checkout is loose, untracked files (surrounding git remote is unrelated). Deploy via the real pokewin-admin repo.

_Creator Hub plan closed. Pick up deferred items below when owner prioritizes._

---

## 📋 Open / next (priority order)

1. ~~Rakeback "total paid" scope mismatch~~ — **RESOLVED (already shipped 2026-06-14 in `53bbb10f`; stale item, confirmed 2026-07-09 part 16):** the PROPOSED surface-wide flip below IS live — all 8 `/insights/rewards/rakeback` modules (`overview`, `active-subscribers`, `cadence`, `daily`, `geo-source`, `rate-distribution`, `roi`, `top-claimers`) use the canonical creator-EXCLUDING scope; `lapsed.ts` (+ `expiry.ts`) remain creator-INCLUDING **by design** exactly as the item required; `_export.ts` no longer exists (refactored away). Re-verified by grep 2026-07-09: zero 2-role fragments left in the rakeback subtree outside `lapsed.ts`. Original entry kept for context:
   **Rakeback "total paid" scope mismatch — PROPOSED, needs owner sign-off (2026-06-14):** the NEW `/rewards/rakeback` instant-claim "total paid" uses canonical `getMetricsScope` (creator-EXCLUDING) but the `/insights/rewards/rakeback` overview headline ("Total rakeback") uses inline `u.role NOT IN ('admin','support')` (creator-INCLUDING), so the same program shows two different totals. Per CLAUDE.md canonical scope (excludes creators) the OLD overview is the inconsistent one. **Read-only prod numbers (lifetime, settled claims):** OLD creator-including = **$13,335.87** · NEW creator-excluding = **$13,300.87** · creator-only delta = **$35.00** (351 claims / 77 creators). **NOT shipped — entangled, not cleanly separable:** the displayed `overview.totalRakeback` is produced by `computeOverview` in `src/lib/queries/insights-rewards/rakeback/overview.ts` in the SAME query/scope that yields `totalWager` (→ "% of wager" KPI), count, claimants, avg, largest + prior-window deltas; ALL 9 sibling rakeback modules (`active-subscribers`, `cadence`, `daily`, `geo-source`, `rate-distribution`, `roi`, `top-claimers`, + the `_export.ts` CSV + the forecast engine via `getRakebackOverview`) share the identical inline creator-including fragment. Aligning ONLY the total would desync it from its own wager denominator + every sibling tab. **PROPOSED patch (owner approve before applying):** flip the whole `/insights/rewards/rakeback` surface to canonical creator-excluding scope — swap the inline `AND u.role NOT IN ('admin','support') ${blacklistJoin}` for the canonical customer scope (`excludeStaffCreatorsAndBlacklistedSql` / `getMetricsScope().userScopeSql`, which is `role NOT IN ('admin','support','creator')` + same blacklist) in all 9 modules consistently, render-verify the whole surface, and report the before/after headline ($13,335.87 → $13,300.87). **DO NOT TOUCH the intentional creator-INCLUDING reconciliation surfaces** `src/lib/queries/insights-rewards/expiry.ts` and `rakeback/lapsed.ts` (left creator-including by design). Impact is tiny ($35) but it is a displayed money total — owner decides whether the consistency change is worth the surface-wide scope flip.
2. ~~Admin-DB schema drift decision~~ — **RESOLVED 2026-07-09 (see SESSION part 15):** the claim was stale — `creator_deals` cashout-limit columns ARE in schema + prod (column-exact match) and `creator_deal_estimates` no longer exists in prod. The REAL drift found instead: two empty orphan tables `employee_discord_server{s,_placements}` (feature `37056bb1`, removed in `f6514a42`; 0 rows, 0 code refs) → DROPPED via `db execute`; plus `admin_withdrawal_unlocks.unlocked_by` FK missing `ON UPDATE CASCADE` (empty table) → aligned. **`prisma migrate diff` now EMPTY (exit 0) — prod admin DB ≡ schema.**
3. Packy.gg avatar write — **BLOCKED** (no confirmed backend endpoint; ADMIN-only pfp preview OK)
4. Bulk delete `/gift-cards` + `/vouchers` — **BLOCKED** (MAIN DB write forbidden)
5. ~~Fold durable reward findings into `ONBOARDING.md`~~ — **DONE 2026-07-09:** affiliate commission basis (share of referred edge/GGR, not wager) + signup $5.71 (amortized-per-signup efficiency metric, not the grant) folded into ONBOARDING §6 from the resolved `system-edge-plan/_model.ts` anchors.
6. ~~Add `/creator-hub/tips-sponsors` to responsive matrix~~ — **DONE 2026-07-09:** `hub-tips-sponsors` added to `CREATOR_HUB_ROUTES` in `e2e/responsive/config.ts` (config-only; sweep not re-run this session).
7. **Retune audit + fix stream (2026-07-02, full audit in `AUDIT_RETUNE.md` — untracked, repo root of the main checkout). SHIPPED:** buildPack→official `5579176c` · shared ±60% band + anchor + tagged value-equality gate at write `b6b28133` · staged-approve honors the price checkbox (F4) `97880e90` · system-balance toggle keeps verdicts (F10) `398449f5` · phased price search covers the whole band + early-stop (RC3, default budget 50→120, harness +2) `1ccecb47` · DB tags column read everywhere + 0.1pp tagged write assert + contradiction refusal + tag-aware bulk/drawer defaults (TAG-2/3/4 + staged tagged gate, harness +1) `91760bf8` · snap acceptance widened to the precise window [target, target+0.001] + honest saturated-anchor error text (RC5a/RC4-text) `ed6d14c8` · **RC1 completion `cee515d5`:** every proposal now carries a `poolFingerprint` (new pure `packs/_lib/pool-fingerprint.ts`, cyrb53 over live price + sorted (cardId,weight) pairs; plan-cache keys bumped v2), review Approve pins `approvedPriceAfter` (proposal or Adjust-panel re-solve — `AdjustedState` now carries its `bestPrice`, confirm gate shows it) + `approvedPoolFingerprint` into `applyPackRetune` AND `applyStagedPackEditAndRetune`; both writes fail closed BEFORE solving on fingerprint drift ("pool changed — refresh") and AFTER solving on any re-solved-price ≠ approved-price (tolerance 0, honest param-skew/nondeterminism message); staged approves pin the price only with the search lever OFF; audits carry `approved_artifact`; new fields optional → drawer/bulk legacy callers unchanged (harness 51/51 + 13/13 green) · **UI-truthfulness cluster** (F8 approve gated on stale/recomputing + items re-seed on fresh payloads `2003a863` · F9/C2-F5 searched bestPrice threaded through Adjust/editor previews + confirm gate, after-risk scored AT the searched price `a5be1658` · C2-F6 verbatim approve blocked at odds-sum ≠100% + explicit renormalize `d59aa740` · F12 weight-panel hidden while editor open + no-2FA guide/page copy + doctor header + ±60% band copy `4a55c814`) · **staged-pool dry-run drives the review card + staged edits persist per pack + plain Approve demoted while staged** `d82e94bb` — owner live pain on "1% 18 PLUS" (staging the fixing cards still showed "infeasible"; editor re-seeded on every reopen): NEW read-only `planStagedRetune` (operator-gated, safeQueryOrNull 30s) is the dry-run TWIN of `applyStagedPackEditAndRetune` via ONE shared `resolveAndShapeStagedPool` (the write throws the resolver's byte-identical refusal messages, and the RC1 fingerprint/price pins are enforced inside the same shared pass → staged preview = staged write); review card's verdict/After-tiles/weight-panel switch to the staged plan (badged "Staged — not pushed", debounced 600ms on add/remove/price/lever change — odds typing never re-plans — + manual Recheck, no state-losing router.refresh on picker close; supersedes F12's hide-while-editing with the truthful staged panel once the plan lands); editor sessions persist per pack in sessionStorage (`pack-studio.retune.staged-pool.v1:<packId>`, survives close/rail-nav/refresh re-seed; "Discard edits" clears, "Hide editor" keeps; cleared after a successful staged/verbatim write); plain Approve (LIVE-pool retune) disabled + toast-guarded (incl. "A" shortcut) while staged edits exist — composes with F8's stale/recomputing gate.
   **RC4/RC5b/c shipped `aa69d4f5`** (saturated hard-tag EV knob + tagged snap mode + buffer polish) — closing the fix stream.

8. **RETUNE V2 — full rebuild SHIPPED + browser-verified (overnight 2026-07-02→03, owner mandate: "rebuild as you want, full auto, no more fixing needed"; design docs LOCAL in `.claude/plans/`: `retune-v2-workspace.md` spec+mandates · `retune-v2-blueprint.md` UI/perf/API · `retune-v2-ruleset.md` research-grounded tag laws).**
   - **Push 1 `87a8effa`** (4 commits): `buildRetuneSearchParams` = the ONE param source for plan + both write arms (plan==write by construction, parity harness); `planPackTune(packId, staged?)` one-brain plan action (cached per pack, tag `pack-retune-plan:<id>`); snapshot-seeded rail query (+tags); per-pack cache invalidation (approve busts 1 plan, not 183); pack-studio access reads React.cache-deduped.
   - **Push 2 `32c58d05`**: the WORKSPACE replaced `/pack-studio/retune` in one revertable commit — rail (attention-sorted, search/filters/tag+off-tag badges, bulk checkboxes) + staged pool table + plan panel + ONE push button with the two-step frozen-artifact confirm (fingerprint + price pins); NO eager 183-pack sweep (was 20–60s CPU-blocked; plans are per-pack on demand); client ships only `computePackRisk` (no solver/dnd-kit; route First-Load 396→371 kB, route JS 54.9→42.8 kB); staged edits keyed per pack, survive navigation.
   - **Push 3 `fa753cc6`+`97e903e4`**: swipe queue + review-card/rail/system-balance/guide/pool-editor/drawer DELETED; `planAllRetunes`/`planSingleRetune`/portfolio-profile + the global retune cache tag removed; doctor row → deep-link `?pack=`, doctor bulk → workspace handoff (`sessionStorage pack-studio.retune.bulk-handoff`); sidebar label "Retune". KEPT for drafts: `getPackEditPool`/`applyPackEdit`/`getRetunePickerFilters`.
   - **Push 4 `39f5750d`…`7f24ca4e`** (8 commits, the ruleset): archetype targets (TAG_CAP_HEADROOM 1.15 → 230×/460×/2300× caps so the owner's 202–207× 10%-jackpots survive; `TAGGED_NEAR_MISS_MIN=0` binary lotteries, NM seeded from the live pool; pool-aware edge curve via `poolMaxWin` — no phantom premium; arbitrary hit-rates incl. decimals; `resolveIntendedHitRateDetailed` w/ mislabel flag); engine: LAW-6 staged-in cards uncapped, one-sided 0.25pp EV-window acceptance (RC4 point-EV wall dead), tagged per-100k house-ladder snap w/ 1-in-N jackpot menu (CLEAN-XOR-TAG dead); NEW pure `insights/edge-calc/tag-guidance.ts`: ranked ENGINE-PROVEN suggestions (price-edge-exact / price-move / add-dust-card band w/ analytic share / add-jackpot / edge-bump / raise-cap / repair-monotone / retag) — every infeasible/dirty tagged plan carries fixes, never bare errors; LAW-15 tagged winRateTol 1e-4 in the shared builder; plan-panel renders suggestions w/ one-click picker seeding; NEW `__checks__/archetypes.ts` 16 checks.
   - **Fleet acceptance (builder + verifier independently reproduced):** 41 tagged: **36 feasible CLEAN** (tag ≤1e-4, edge in [curve, +0.25pp], per-100k snapped) · 3 dirty w/ solver-verified price suggestion · 2 infeasible w/ retag+edge suggestions (the genuinely mis-tagged pair: Divine Order %10 living @30%, Lucky Pond %5 @10%) · **0 bare errors**. Fleet-wide clean share **80.9%** (was 32%). Untagged: 141/142 feasible ("Rule Set" errors w/ legacy suggestion copy).
   - **Logged-in browser pass DONE (owner-sanctioned, this session):** load fast → rail/search/filters → deep-link `?pack=` (works after hydration) → "1% 18 PLUS" plans w/ ranked COMPUTED FIXES (price→$1.26 @11.486% tag-exact · add dust $0.01–$0.08 suggest $0.03 ≈5.5%) → "1% Bidoof" clean plan ($3.33→$3.32 "so the odds land clean", on-tag, "Ready. Clean odds") → Push → step-1 frozen artifact → step-2 "100% sure" → **CANCELLED — zero MAIN writes executed by any agent all night** → console clean (only a crypto-extension error). "Pushed this session: 0".
   - **OPEN (small):** owner decision on retagging Divine Order (%10→30%?) + Lucky Pond (%5→10%?) — the workspace suggests it; blueprint §10 deferred list (verbatim odds live in Drafts); Push-4 gaps: ~~ruleset delta 9~~ (segment-seeded candidate search — SHIPPED `05e319f9`, wave 6) + some dedicated harness sub-checks cross-validated instead; TAG-5 policy (verbatim drafts push has no tag guard, deliberate); a `.env` oddity found during validation: `DATABASE_URL` appears TWICE (first→45-pack DB, last→real prod; dotenv/Next take the LAST — scripts must too).

9. **V2 POLISH WAVE (2026-07-03, owner-driven, all adversarially verified + pushed):**
   - **Untagged degenerate-ladder guidance `636e542d`** (owner case pack Captive): `computeUntaggedGuidance` — floor-pinned rows get a "min" chip + tooltip; amber banner with engine-proven fixes (add-mid-card band w/ analytic share, remove-dead-cards, accept-as-is with the numeric why: loss avg needed vs carrier value). Harness +5 (Captive fixture). Known nuance: the enriched-Captive fix needs the PIN-PRICE flow (free search drifts; NO snapped combo exists for that pool — 165-combo sweep) → suggestion copy states it; push of that fix routes via pins/Drafts or an owner-approved exact-odds override (NOT built — owner call).
   - **Shell speed `a86eb024` + `d53209d8`** (perf autopsy R3): `verifySession` 2 serial admin reads → 1 combined (P2022-safe fallback); pack-studio layout starts all post-session reads in ONE parallel wave, gate still decides BEFORE any JSX (security-audited line-by-line, defense-in-depth via per-page gates); `resolveLiveSession` React.cache'd in pack-studio + creator-hub access libs (3 reads/request → 1). Waves: studio 4→2, admin 3→2. Follow-up NOT applied by design: Suspense-streaming profile/prefs (TimezoneProvider/header coupling, ~0 gain).
   - **PINS `3c4f2191..a46722c4`** (owner wish: hand-edit odds): `shapeWeights.pinnedShares` — owner-typed per-card odds bind plan AND write (PIN_SCALE 1e9 integer-exact, residual pool solves around pins, snap never moves a pin, `pins-infeasible` limit w/ computable remedies incl. raise-cap); `pinnedOdds` through the one-brain builder + staged plan/write + frozen artifact + audit; UI: click-to-edit Planned %, pin chips, clear-all, sessionStorage persistence. NEW harness pins.ts 13 checks + MASAKI fixture (= pack "Heavy Hitters" $0.95 %1: pin on the $2,507 Machamp → cap-refusal naming the card; counterfactual cap $2,600 → pin held at exactly 0.0005%, tag exact). Deliberate narrowing: the LIVE write arm (`applyPackRetune`) takes no pins — any pin makes the edit staged by construction.
   - **SNAP NICENESS `d056b2d9..53f12ff7`** (owner: "not fully clean yk", pack = mis-tagged "5% Lucky Pond"): tagged snap → three-tier N/P/G search on the HUMAN-NICE grid (42 rungs, mantissas {1,1.5,2,2.5,3,3.5,4,5,7.5}, 1-in-N jackpot menu); `niceTier` scoring between snapPriority and centsDist; `allNice` + honesty banner when a saturated pool can't be prettified (push stays enabled); tagged `offLadderCards` now means off the NICE grid; pinned cards exempt. Also: "Rule Set" fixed via `priceIndependent` limit flag (searched 120→1, honest price-free copy). Fixtures bit-exact: Lucky Pond 5% → **$1.82, odds 0.35/0.4/0.5/0.5/0.75/2.5/95**; 10% → $2.69 unique all-nice. Fleet re-run: tagged clean-snapped 32→35/37, 25/35 all-nice, 0 regressions. NEW harness niceness.ts 18 checks — **six suites, 131 checks total**. Note: pins.ts counterfactual price legitimately moved $1.17→$0.98 (niceness re-picks; pin/tag contract unchanged); live-plan cache key now v4.

---

## 🔴 Blocked (needs owner)

| Item | Why | Options |
|---|---|---|
| Bulk delete `/gift-cards` + `/vouchers` | Tables in **MAIN DB** — write forbidden | H1: allow MAIN write · H2: gift-cards admin cancel only · H3: drop |
| Packy.gg PFP update on Add Creator | MAIN write / no API | ADMIN-only preview until backend endpoint exists |

---

## ⚠️ Gotchas (session-relevant)

- **Dev-server PORT collision across worktrees (2026-06-14)** — multiple parallel worktrees run `next dev`/`next start`, all defaulting to **:3000**. The first to bind wins; a later `npm run dev` silently fails to bind and your Playwright/curl then hits the OTHER worktree's STALE code (you'll see pre-edit labels render despite a clean local diff + fresh `.next`). FIX: render-verify on a unique port (`PORT=3100 npm run dev`) and confirm the :PORT listener's `CommandLine` matches YOUR worktree path before trusting any render. Also: start the dev server via the Bash tool's `run_in_background:true` (a `&`-detached `npm run dev` dies when the tool call returns on Windows).
- **Stale local game DB** — live admin pages throw locally → use fixtures (`src/app/responsive-fixture/*`) or prod; Creator Hub e2e detail tab skips when no `creator` role user in MAIN
- **Backend API env** — Hub dashboard/roster/deal-tracker degrade gracefully when `BACKEND_API_URL_*` missing (KPIs show `—`); not a render failure
- **Admin DB = `db push` only** — never `prisma migrate dev/deploy` (destructive reset); additive enum: `prisma/admin/sql/20260606_add_creator_manager_role.sql`
- **MAIN DB = read-only** — no schema changes, no writes; `gift_cards` + `vouchers` live in MAIN
- **React #130** — register new nav icons in `app-sidebar.tsx` ICONS map
- **PowerShell UTF-8 BOM** breaks `.sql` — write SQL via Bash/`printf`
- **Verify agents** — `git fetch && checkout exact SHA` before "not found" verdicts
- **Twitter API shape** — read `core` + `avatar`, not only `legacy` (`src/lib/creator-hub/`)
- **Hub ads e2e** — `readSampleAdCode()` needs `admin_settings.house_affiliate_user_id` + a row in MAIN `affiliate_codes`; local VM has neither → detail e2e skips; ads tab still renders house-setup empty state
- **Prod Playwright mint** — cookie signed with VM `SESSION_SECRET` must match Vercel env or prod smoke lands on `/login`
- **Wager-requirement backend dependency** — the 5 default knobs + per-user override are owned by the backend (`packy-backend` branch `axecutioner/sweepstake`, `src/routes/v1/admin/user-wager-requirements.ts`); the panel writes them via `backendApi` (NOT the MAIN DB). Until that branch deploys, both UI surfaces show the muted "awaiting backend deploy" state. New audit event types: `wager_requirement_defaults_updated`, `user_wager_requirement_updated`, `user_wager_requirement_cleared`.
- **Multiplier wager-weights backend dependency** — same "awaiting backend deploy" pattern (`/admin/multiplier-wager-weights`, backend branch in parallel development, NOT in the local packy-backend checkout). Audit event type: `multiplier_wager_weights_updated`. Runtime constants for the client card live in `src/lib/backend-api/multiplier-wager-weights-shared.ts` (deliberately NOT server-only). PUT semantics: tiers replace the stored list wholesale — never send a partial tier list.
- **Crypto-fees backend dependency** — same "awaiting backend deploy" pattern (`/admin/crypto-fees`, backend branch in parallel development, NOT in the local packy-backend checkout). Audit event type: `crypto_fees_updated`. The 11-asset constant lives in `src/lib/backend-api/crypto-fees-assets.ts` (deliberately NOT server-only — client card imports it as a value).
- **Leaderboard wager-weights backend dependency** — same pattern, same backend branch (`src/routes/v1/admin/leaderboard-wager-weights.ts`): 3 knobs `leaderboard_wager_weight_{packs,battles,upgrader}_bps` in MAIN `site_config` (hence the `movedKeys` filter), written ONLY via `backendApi`. Audit event type: `leaderboard_wager_weights_updated`. Card degrades to "awaiting backend deploy" until that branch ships.
- **Fresh checkout may lack `node_modules` + `.env`** — run `npm install` (NOT `npm ci` — lockfile mismatch) then `prisma generate` for BOTH schemas before tsc/build; without `.env` the minted-session Playwright harness can't run (needs `SESSION_SECRET` + `ADMIN_DATABASE_URL` + `DATABASE_URL`).
- **Fire-and-forget `void compareX(…)` hooks do NOT flush in `next dev` (2026-07-09)** — post-response work is dropped locally, so a missing `[ch-compare]` line in a dev-server log is NOT evidence the hook is broken (awaiting the same call in a temp route logs fine; prod/Vercel is the real surface). Verify comparison hooks via an awaited temp probe route, not the page render.
- **App-Router `_`-prefixed segments are PRIVATE (2026-07-09)** — a temp `/api/_probe/route.ts` 404s silently; name temp probe routes without the leading underscore (and delete them after use).

---

## 🧰 Doc index

| Need | File |
|---|---|
| Read/write protocol (forced) | `SESSION_MEMORY.md` |
| Architecture + domain | `ONBOARDING.md` |
| Work rules | `AGENTS.md` · `CLAUDE.md` · `CLAUDE.local.md` |
| Creator Hub plan + progress | `.claude/plans/iridescent-mixing-lecun.md` |
| Layout audit | `AUDIT_REPORT.md` |
| Responsive harness | `e2e/responsive/*` · `playwright.responsive.config.ts` |
| Creator Hub e2e smoke | `e2e/tests/creator-hub.spec.ts` |
| Full historical detail (pre-2026-07-12) | `AGENT_HANDOFF_ARCHIVE.md` |
