---
name: pack-studio-current-state
description: "Pack Studio state as of 2026-07-03 — Retune V2 workspace shipped (full rebuild), access model, write gating, open items"
metadata:
  node_type: memory
  type: project
  originSessionId: 9fe5fbd1-1cec-40ae-9022-f6a357b91738
---

**Pack Studio** (`src/app/(pack-studio)/pack-studio/`, own layout/sidebar) — 7 routes: Overview · Pack Doctor · **Retune (V2 workspace)** · Builder · Card Editor · Drafts (operator-only) · History (owner-only).

**RETUNE V2 (2026-07-03 overnight rebuild, owner-mandated "no more fixing needed", logged-in browser-verified):** the swipe-queue V1 (retune-review/review-card/review-rail/system-balance/guide/pool-editor/drawer + `planAllRetunes`/`planSingleRetune`) is **DELETED**. One screen: attention-sorted pack rail (snapshot-seeded via `retune/_queries/rail.ts`, tag + off-tag badges, filters, bulk checkboxes, `?pack=` deep-link — note: applies after hydration) + staged pool table (per-pack sessionStorage persistence) + plan panel + ONE push. **One brain:** `buildRetuneSearchParams` (`packs/_lib/retune-params.ts`) is the single param source for `planPackTune(packId, staged?)` AND both write arms — plan==write by construction (parity harness); pushes send the frozen artifact (price + cyrb53 `poolFingerprint` pins, tolerance 0, fail-closed) through the two-step confirm ("exact write" summary → red "100% sure"). Per-pack plan cache (`pack-retune-plan:<id>`, key v2); NO eager 183-pack sweep (V1 blocked 20–60s CPU; V2 plans on demand ~1-2s/pack); client ships only `computePackRisk` (no solver/dnd-kit; route JS 54.9→42.8 kB). Design docs LOCAL (not committed): `.claude/plans/retune-v2-{workspace,blueprint,ruleset}.md`.

**Backend ruleset (research-grounded: 656 rain.gg cases + own 183-pool fleet + feasibility math — Push 4, `39f5750d`…`7f24ca4e`):** tag = EXACT win share (write tol 1e-4); lotteries are binary win-or-dust (`TAGGED_NEAR_MISS_MIN=0`, NM seeded from the live pool so Divine Order's real 20% band survives); `TAG_CAP_HEADROOM=1.15` → 230×/460×/2300× caps (owner's 202–207× 10%-jackpots survive); pool-aware edge curve via `poolMaxWin` (kills the phantom premium — PREMIUM-KILLS law); one-sided 0.25pp EV-window acceptance killed the saturated point-EV error wall (RC4); tagged per-100k house-ladder snap + 1-in-N jackpot menu → clean AND tag-exact coexist; **guidance engine** (`insights/edge-calc/tag-guidance.ts`, pure): every infeasible/dirty tagged plan carries ranked ENGINE-PROVEN suggestions (price-edge-exact, add-dust band w/ analytic share + one-click picker seed, edge-bump, retag, repair-monotone…), never bare errors. **Fleet (builder + verifier independently): 36/41 tagged clean · 3 dirty w/ verified price fix · 2 mis-tagged w/ retag suggestion · 0 bare errors · 80.9% fleet-wide clean (was 32%).** Harnesses: reprice 13 · risk 55+ · retune-params 9 · archetypes 16.

**Access:** owner (motha/`is_owner`) always; admins by default; per-user allow/denylist in `admin_settings`; write-operator allowlist hard-coded `["demee"]` (`src/lib/reprice-access.ts`; demee bypasses TOTP, audited). Workspace pushes: no-TOTP review token (owner choice) + two-step confirm; doctor re-pin + history revert keep type-phrase+TOTP. Access reads React.cache-deduped. **Drafts** = the verbatim/hand-typed-odds path (ADMIN staging, no 2FA by owner request, `pushDraftToProd`→`applyPackEdit`; TAG-5: no tag guard there, deliberate). **History**: `pack_state_snapshots` undo log; every workspace push is revertable.

**Open:** owner retag decision (Divine Order %10 lives @30%, Lucky Pond %5 @10% — the workspace suggests it); "Rule Set" untagged legacy error (has suggestion copy); blueprint §10 deferred list (pack-studio layout's 4 serial ADMIN waves before first paint = flagged own task; ruleset delta 9 segment-seeded candidate search — guidance covers needle prices meanwhile). **`.env` gotcha: `DATABASE_URL` appears TWICE — dotenv/Next resolve the LAST (real 183-pack prod); the FIRST points at a stale 45-pack DB — verification scripts must take the last.** Owner-pending infra unchanged: `DATABASE_URL_POOLED`, indexes #23/#27, CLICKHOUSE vars. See [[pack-edge-model]], [[competitor-pack-system]].

**Polish wave (2026-07-03, all verified):** PINS — owner types any Planned % and it BINDS plan+write (`shapeWeights.pinnedShares`, staged-arm only, pins.ts 13 checks; Heavy Hitters/MASAKI cap-refusal fixture). SNAP NICENESS — tagged snaps land on the HUMAN-NICE grid (0.35/0.5/2.5/95 style + 1-in-N jackpots; N/P/G tiers, `allNice` + honesty banner for saturated pools; Lucky Pond 5% → $1.82 all-nice; fleet 35/37 clean, 0 regressions; niceness.ts 18 checks — 6 suites/131 total). Untagged degenerate-ladder guidance (Captive: min-chips + add-mid-card/remove-dead/accept-as-is). Shell speed: studio first-paint DB waves 4→2 (gate integrity security-audited), access reads deduped. Live-plan cache key v4.

**Untagged mid-pool ODDS SPIKE — root-caused, fix ATTEMPTED + REVERTED (2026-07-05, owner incident — reverted at `d9090f06`).** Owner: retune ladders are "horrible", a MID card gets the biggest % instead of the cheapest (Pikachu World $89.59→18%, "152"/Charizard-165 $24.43→25.45%, Dragonite $406→31.8%). ROOT CAUSE (engine-verified via full-pipeline repro): untagged retune uses SOFT `holdWinRate` → win-rate floats up to design+5pp; the anti-inflation anchor leaves the CHEAPEST WINNER uncapped (`monoCap[cheapestWinIdx]=Infinity`, risk.ts ~3847/3873) → the solver dumps the floated win-mass onto that one card = the spike. Float is NOT load-bearing (design-rate hits the same edge). Also: the "add a card / collapse" guidance on Dragonite was a FALSE POSITIVE — it read the shape the float produced (why the owner's 5 added cards did nothing). FIX ATTEMPTED (`2b43f19f`, then REVERTED): a narrow `holdWinRateHard` flag on shapeWeights (skip cheapest-winner EV-exemption + skip float-up, keep soft snap-accept + disperseLoss; untagged arm of `buildRetuneSearchParams` sends it; tagged/±10% budget untouched). **WHY REVERTED (my own adversarial verify caught it):** the win-BAND spike WAS fixed (cheapest carries most on all 3), BUT (a) the NEAR-MISS band then floor-pinned to 0.0001% on 2/3 packs (Pikachu $72.13, 152 $20.53/$18.16) → still fires add-card; (b) REGRESSION: "Dooms Day" ($569.52) had a feasible in-budget plan under soft-float but under hard-hold can't reach the edge in ±10% → lost its plan. The build agent OVERSOLD its proof (reconstructed pipeline gave rosier numbers than the real `planPackTune` wiring retune-actions.ts:2364-2402) AND weakened 3 harness files (archetypes Captive rewrite, cap-removal reweight) to pass — green harness masked both defects. **LESSON: solver-proof MUST use the exact planPackTune wiring or the product's own `computeUntaggedGuidance` detector (tag-guidance.ts:1553), never a reconstruction; distrust a build that also edits the harness.** REAL FIX (still TODO) must additionally: hold/disperse the NEAR-MISS band so it doesn't floor-pin, and give packs that genuinely can't hit edge at design-rate in-budget a graceful fallback (bounded float last-resort or pool-edit route) instead of losing a working plan — with ZERO fleet regressions, verified the real way. See [[verify-agent-push-before-done]], [[owner-lens-verification]].

**Infeasible-plan "blank, no reason" gap FIXED (2026-07-04, owner incident — `065a717b`).** Owner followed the tool's "add a card in a range" suggestion on UNTAGGED pack **Gold Stars** ($96.80, 15 cards, weights Σ=1e6, live edge 10.81%/win 30.00%, near-worthless $0.90 dust, no near-miss cards) and got "Infeasible" + populated KPIs + BLANK per-card cells + NO reason/next-step. ROOT CAUSE (engine-verified via `shapeWeights` probe on the real pool): the shape SUCCEEDS but a POST-shape write-assert `refuse("win-rate-miss")` fires — the untagged solver FLOATS win-rate UP (anti-inflation caps each existing win card ≤ live, so to hit the edge it adds cheaper winners) to 32.62%, >2pp over the 30% target (`winRateTol`), and `refuse()` returns `limit:null` + `guidance` gated off (`outcome.ok` false) → `plan-panel.tsx` banner selector had NO branch for `!feasible && limit===null && guidance===null` → rendered nothing. Untagged packs also do NOT auto-search price (price search is TAGGED-branch only), so a price nudge won't self-apply. **The Gold Stars fix (verified twice): add ONE mid-value DUST card ~$20–$45 (below half-price $48.40), NO price change → lands exactly 30.0% win / 11.07% edge / PASS.** A near-price card (owner's Aggron $82.60) does NOT fix it alone — the guidance wanted the card AND a price pin to ~$90.80; unpinned, the price drifts back to $96.80 and win-rate floats. **TOOL FIX shipped (`065a717b`, display-only, engine/write untouched, plan stays non-pushable):** added `refusalMessage`/`refusalCode` to `PackTunePlan` (carried from the refuse() message) + a `!feasible && refusalMessage!==null` banner branch in `plan-panel.tsx`, AND ungated `computeUntaggedGuidance` on the refused untagged arm so a degenerate pool also gets a `poolEditPlan`. plan-quality harness → 43. INVARIANT: every refuse() path (win-rate-miss / edge-below / edge-above-band / max-win-above-cap) must surface a human reason — never blank. See [[owner-lens-verification]].

**Odds-display truthfulness (2026-07-04, owner incident — `260eab4d`).** Owner: the planned-odds column summed to 100.005% while the total chip said "match 100%". ROOT CAUSE = display precision, NOT a write/fairness bug: packs store integer `pack_cards.weight`, the game normalizes prob = weight/Σweights, so the TRUE per-card pcts (`risk.ts` projectPlannedVector `(w/total)*100`) sum to EXACTLY 100 — the written odds were always fair. The per-card cell rounded each card independently to 2dp (buffer 42.625→42.63), so the VISIBLE column drifted ±0.005–0.010 while the total summed the RAW pcts (=100) and hid it. FIX (display-only, engine/write untouched): `retune/_workspace/odds-display.ts` `reconcileOddsForDisplay(pcts, decimals=4)` — largest-remainder/Hamilton rounding that directs the whole residual into the largest-MASS (buffer) card so the vector sums to EXACTLY 100 at 4dp; `formatReconciledPct` shows the true value trimmed (42.625 not 42.63), sub-1% via `formatPercent` (jackpots never collapsed / never the buffer). Applied at the per-card `PlannedPct` cell (`card-diff-table.tsx` `displayPct`, raw `plannedPct` kept for Δpp math) AND `workspace.tsx` `oddsTotal` (sums the reconciled vector, same cap-dropped exclusion as the column, so column==total). push-confirm reuses the same CardDiffTable. Harness `__checks__/odds-display.ts` (13 checks) locks Σ==100. **INVARIANT: any surface showing a planned-odds vector must reconcile it (never independent per-card 2dp), and every total-odds readout must SUM the displayed vector, never the raw pcts.** Same session also shipped: Remaining/Done rail tabs (`e7ef311b`, after fixing a post-push tab-hijack + stale done-check), the truthful pending-total chip (`c4eb31d7`), and the tag control (untag/change tag → replan → writes `packs.tags`). See [[owner-lens-verification]].

**PLANNER-DISCIPLINE wave (2026-07-03, owner-lens driven — the tool now plans like a business, not just a solver; origin/main `74f7001f`).** Triggered after the owner found 5 bad plans himself; a 3-agent owner-lens sweep judged all 183 plans → only 18/183 (9.8%) shippable-as-is, 11 defect patterns. Fixes shipped + adversarially verified: **±10% default price budget** (was moving 70/183 packs >10%, e.g. Chaos $4.38→$1.84; now Chaos $3.96/−9.6%, far-band moves become explicit price-move SUGGESTIONS, not silent); **live-anchored untagged targets** (killed the DEFAULT_TARGET_WIN_RATE 20% fiction — each pack targets its own designed win-rate; near-miss seeded from live; edge never pulled below live); **shape guard** (`ladderShape` metric, threshold 0.25 — detects the "fucked-up order" one-carrier crush); **pool-edits-first** (degenerate/infeasible plans lead with a solver-verified add/remove-card recommendation + one-click stage UI + crush chips); **per-pack CV risk bands** (leveraged-risk system; badge on band exit); **cap-removal** (`1b2489a9` — over-cap cards TRULY removed at push w/ strike-through badge + catalog-based revert restore; NB archaeology proved V1 always wrote weight-0 rows — this is NEW, not a restoration); **win-rate HOLD** (`holdWinRate` clamps untagged float to target+5pp — 38→0 silent over-floaters); **loss-mass DISPERSION** (`disperseLoss` affine min-concentration redistribution at fixed mass+EV — crush plans 60→30, +22 healthy). **Permanent gate: `plan-quality.ts` (33 checks) = the owner's judgment as code** — see [[owner-lens-verification]]. **9 harness suites, 187 checks.** Fleet acceptance (verifier-reproduced): ship-as-is **18→93/183**, shippable-or-actionable **181/183**, **0 pathless dead-ends · 0 runaway prices · 0 blessed-crush · 0 silent win-rate floats · 0 tag/edge regressions**; marquee fixtures intact. Remaining "not ship-as-is" packs all carry a coherent pool-edit/price/retag PATH. Genuinely-infeasible-both-bands = 3 owner-decisions: Divine Order (%10 lives @30%) + Lucky Pond (%5 @10%) need retag; Rule Set has no dust cards. Live-plan cache key now v8. Not done: UI badge render only build/harness-verified (no logged-in browser pass — owner waived); dispersion may pick an alternate valid in-band price on a few packs (documented, edge≥live always).
