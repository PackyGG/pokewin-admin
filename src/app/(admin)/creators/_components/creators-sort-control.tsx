"use client";

/**
 * Placeholder export — the sort control was previously a Recent / PnL
 * dropdown but the PnL-based options were removed when the per-card
 * PnL display was dropped from /creators. The control no longer adds
 * value (Recent is the only mode), so the file is kept only as a
 * no-op stub to avoid touching every import site.
 *
 * If a future sort mode lands (volume, signups, etc.), re-introduce
 * the dropdown here.
 */
export function CreatorsSortControl() {
  return null;
}
